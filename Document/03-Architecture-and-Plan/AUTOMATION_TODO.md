# Automation Development To-Do

> สถานะ: **Automation Work Tracker — Single Source of Truth**
>
> อัปเดตล่าสุด: 26 สิงหาคม 2026
>
> ขอบเขต: Automation UI, API, Domain, Database, Windows Agent, DSL, Queue, Evidence, Test Data, Regression และ CI/CD

ไฟล์นี้ใช้ติดตามงานพัฒนา Automation ที่ยังเหลืออยู่ ทุกครั้งก่อนเริ่มงาน Automation ต้องอ่านไฟล์นี้ทั้งไฟล์ และเมื่อทำงานเสร็จต้องอัปเดตสถานะกับ Progress Log ภายในงานเดียวกัน

## 1. วิธีใช้สถานะ

| สถานะ | ความหมาย |
|---|---|
| `TODO` | ยังไม่ได้เริ่ม |
| `IN PROGRESS` | เริ่มทำแล้วแต่ Acceptance Criteria ยังไม่ครบ |
| `BLOCKED` | ทำต่อไม่ได้และมี dependency ที่ระบุชัดเจน |
| `DONE` | ทำครบและมีหลักฐานการตรวจสอบแล้ว |

กฎการอัปเดต:

- ระบุผู้รับผิดชอบในช่อง Owner เมื่อเริ่มงาน
- รายการ `DONE` ต้องใส่หลักฐาน เช่น test, endpoint, screenshot, build หรือไฟล์ที่เปลี่ยน
- หาก requirement เปลี่ยน ให้แก้ Acceptance Criteria ก่อนเริ่ม implementation
- งาน UI ต้องอ่าน `UI_DESIGN_SYSTEM.md` และตรวจ Desktop/Mobile ตาม `AGENTS.md`
- หลังแก้ frontend ต้องรัน `npm.cmd run build`, `npm.cmd run lint` และ `git diff --check`
- หลังแก้ backend ต้อง build และ restart API ก่อนตรวจ endpoint จริง

## 2. Baseline ปัจจุบัน

ข้อมูลจากฐานข้อมูล ณ วันที่ 26 สิงหาคม 2026:

| Metric | จำนวน |
|---|---:|
| Automation Cases | 11 |
| Ready | 5 |
| Maintenance Required | 5 |
| Draft | 1 |
| Automation Versions | 13 |
| Action Library | 39 |
| Object Repository | 5 |
| Agents | 1 |
| Executions | 35 |
| Passed | 12 |
| Failed | 23 |
| Evidence | 28 |

ข้อสังเกตตั้งต้น:

- Execution ล้มเหลว 23 จาก 35 ครั้ง หรือประมาณ 66%
- Case 5 จาก 11 รายการอยู่สถานะ `MaintenanceRequired`
- Object Repository มีเพียง 5 รายการ เทียบกับ Action Library 39 รายการ
- ควรทำ Runtime/Selector/Failure ให้เสถียรก่อนเพิ่ม Schedule และ Trigger อัตโนมัติ

## 3. P0 — Runtime และความเสถียร

| ID | งาน | สถานะ | Owner | Acceptance Criteria / หลักฐาน |
|---|---|---|---|---|
| AUT-P0-001 | เพิ่ม Edit Action Library | DONE | Codex | แก้ Name, Category, Description, Parameter Schema JSON, Handler และ Minimum Agent Version ผ่าน modal; server ตรวจ JSON; `AutomationManagementTests`; build/lint + unit tests 70 ผ่าน (2026-08-26) |
| AUT-P0-002 | เพิ่ม Activate/Deactivate Action | DONE | Codex | เปิด/ปิดจาก row action พร้อม confirm; backend update `IsActive`; Validator ใช้เฉพาะ Action active; build/lint + unit tests 70 ผ่าน (2026-08-26) |
| AUT-P0-003 | เพิ่ม Edit Object Repository | DONE | Codex | แก้ Application, Screen, Object Code/Name, Control Type, AutomationId, Selector JSON; ป้องกัน Business Key ซ้ำ; ObjectVersion เพิ่ม; build/lint + unit tests 70 ผ่าน (2026-08-26) |
| AUT-P0-004 | เพิ่ม Activate/Deactivate Object | DONE | Codex | เปิด/ปิดจาก row action พร้อม confirm; endpoint activate/deactivate; Validator ใช้เฉพาะ Object active; build/lint + unit tests 70 ผ่าน (2026-08-26) |
| AUT-P0-005 | Import Object จาก UI Inspector/AutomationId Scanner | DONE | Codex | เพิ่ม modal Import Scanner ใน Object Repository รองรับ JSON/CSV, Preview diff, เลือกเฉพาะแถว Ready, ป้องกัน Business Key/AutomationId ซ้ำฝั่ง UI และ API; frontend build + backend build ผ่าน (2026-08-26) |
| AUT-P0-006 | Object Verification Tool | DONE | Claude | ตาราง `AutomationObjectVerifications`; endpoint `POST objects/verify`/`GET objects/verifications` (Hub) + `POST verifications/claim`/`verifications/result` (Agent); Agent `ProMaxx2.Automation.Core/ObjectVerifier.cs` เทียบ AutomationId/ControlType จาก `UiInspector` scan คืน Found/NotFound/Duplicate/ControlTypeMismatch พร้อมเวลา+Agent; คำสั่งใหม่ `runner verify --exe <path>`; UI: เลือก Object หลายรายการ + ปุ่มตรวจสอบ + modal ผลตรวจสอบใน Object Repository; **หมายเหตุ: Agent build ผ่านแล้วแต่ยังไม่ได้ทดสอบกับ ProMaxx2.exe จริง ต้องให้ QA ทดสอบภาคสนามบนเครื่องที่มี AUT ติดตั้งจริง** (2026-08-26) |
| AUT-P0-007 | ซ่อม Case ที่เป็น MaintenanceRequired | DONE | Claude | `AutomationCase` เพิ่ม MaintenanceReason/Owner/OpenedAt; `RequireMaintenance` บันทึกสาเหตุอัตโนมัติจาก ErrorMessage ตอน Complete; endpoint `POST cases/{id}/maintenance/owner`/`maintenance/resolve` (resolve → กลับสถานะ NeedsReview บังคับ Validate/อนุมัติใหม่); UI panel ในหน้า Case Detail (2026-08-26) |
| AUT-P0-008 | วิเคราะห์ Execution Failed เดิม | DONE | Claude | Persist `ClassifiedFailureType`/`ClassifiedRecommendation` บน `AutomationExecution` อัตโนมัติทุกครั้งที่ Complete ด้วย `AutomationFailureClassifier` เดิม; endpoint `GET failures/dashboard`/`failures/executions` กรอง/จัดกลุ่มตาม Failure Type, Build, Agent, Automation Case (2026-08-26) |
| AUT-P0-009 | Retry Policy | DONE | Claude | ตาราง `AutomationRetryPolicySettings` (MaxAttempts/BackoffSeconds/Enabled, default 2/30s); `AutomationAction.RetrySafety` (Safe/Unsafe/Conditional); auto-retry ใน `CompleteExecutionAsync` เมื่อ Recommendation เป็น Retry/RetryOrCheckEnvironment และไม่มี Unsafe step สำเร็จ, backoff ผ่าน `AutomationJob.QueuedAt` ในอนาคต + filter ใน `ClaimNextJobAsync`; endpoint `GET`/`PUT settings/retry-policy`; UI แสดง Retry Count บน Execution/Job และตั้งค่า Retry Policy ในหน้าจัดการ (2026-08-26) |
| AUT-P0-010 | Flaky Test Quarantine | DONE | Claude | `AutomationCase` เพิ่ม IsQuarantined/QuarantineReason/Owner/ExpiresAt (แยกจาก Status หลัก); `GetFlakyCandidatesAsync` ตรวจ transition Pass/Fail ≥2 ครั้งใน 5 execution ล่าสุด; endpoint `GET cases/flaky-candidates`, `POST cases/{id}/quarantine`/`unquarantine`; UI panel "Flaky Candidates" ในหน้า Cases + badge Quarantined (2026-08-26) |
| AUT-P0-011 | Failure Dashboard | DONE | Claude | Tab ใหม่ "Failure Dashboard" กรอง Failure Type/Build/Agent/ช่วงวันที่ พร้อม breakdown by Build/Agent/Top Automation Case และตาราง drill-down เปิด Execution Detail เดิม (2026-08-26) |
| AUT-P0-012 | Runner ส่ง execution-level ErrorCode ที่แท้จริงแทนการ hardcode | DONE | Claude | แก้ `agent/ProMaxx2.Automation.Runner/Program.cs:ExecutePackageAsync` — เก็บ `StepOutcome` ของ step ที่ fail (`failedStep`) แล้ว forward `failedStep.ErrorCode`/`failedStep.ErrorMessage` จริงไปที่ `client.CompleteAsync` แทนการ hardcode `"AUT-UI-003"`/ข้อความ generic เดิม (fallback เป็น AUT-UI-003 เฉพาะกรณี `failedStep` เป็น null ซึ่งไม่ควรเกิดขึ้นจริงเมื่อ `overall=false`); ตรวจแล้วว่า raw `FailureType` field ที่ agent ส่งไม่ถูกใช้ใน retry decision เลย (มีแค่ `ClassifiedFailureType` ที่ server คำนวณเองจาก `ErrorCode`) จึงคงค่า `"AutomationFailure"` ไว้เหมือนเดิมไม่กระทบ logic; agent solution build 0 warning/0 error (2026-08-27) |
| AUT-P0-013 | DB Validator: ConnectTimeout + CancellationToken ไม่ถูกใช้จริง | DONE | Claude | เพิ่ม `DbProfile.ConnectTimeoutSeconds` (default 5, คำนวณจาก `Math.Min(config.ActionTimeoutSeconds, 5)` ใน `FromEnvironment`) set บน `FbConnectionStringBuilder.ConnectionTimeout`/`SqlConnectionStringBuilder.ConnectTimeout`; เพิ่ม `SqlConnectionStringBuilder.ConnectRetryCount=0` ป้องกัน SqlClient หน่วงเวลาซ้ำด้วย internal reconnect-retry (ทดสอบจริงพบว่าตอนแรกที่ตั้งแค่ `ConnectTimeout=10` โดยไม่ปิด retry ยังใช้เวลาเต็ม 10s ตาม cap พอดี ไม่เร็วกว่านั้น — ปิด retry แล้วจึงมั่นใจได้ว่าเวลาที่ fail จะไม่เกิน ConnectTimeout ที่ตั้งไว้จริง); เปลี่ยนทั้งสอง validator เป็น async จริง (`OpenAsync(ct)`/`ExecuteReaderAsync(ct)`/`ReadAsync(ct)`) ให้ `CancellationToken` มีผลจริง; เพิ่มเทสต์ยืนยัน: SQL Server connection-refused ตอนนี้ fail ใน ~5s (ลดจาก ~14s เดิม), `ConnectTimeoutSeconds` cap ถูกต้องตาม ActionTimeout สั้น/ยาว; agent test suite 48/48 ผ่านใน ~7s (2026-08-27) — **หมายเหตุ: fix นี้เองมี regression เล็กน้อยที่ถูกจับได้ตอน code review รอบ merge PR #1 แล้วแก้ต่อใน AUT-P0-014** |
| AUT-P0-014 | Code review ก่อน merge PR #1: แก้ 7 bug จริงที่พบ | DONE | Claude | รัน `/code-review` (high, 7 finder agent) บน PR #1 ทั้งหมด (8 commits) ก่อน merge เข้า main ตามที่ผู้ใช้ขอ — พบและแก้ bug จริง 7 จุด: (1) **Quarantine bypass** — `RequestExecutionAsync`/`BatchRunAsync`/auto-retry ไม่เคยเช็ค `IsQuarantined` เลย มีแค่เช็ค `Status=="Ready"` ทำให้ case ที่ quarantine ไปแล้วยังรันได้ปกติ เพิ่มเช็คครบทั้ง 3 จุด; (2) **Maintenance whitelist ไม่ตรงกับ classifier** — `CompleteExecutionAsync` เดิม gate `RequireMaintenance` ด้วย hardcoded `Status=="Failed" && ErrorCode is AUT-UI-00x` เท่านั้น ทั้งที่ `AutomationFailureClassifier` (ที่ PR เดียวกันเพิ่มเข้ามา) แนะนำ MaintenanceRequired ให้ AUT-DSL-001/AUT-AI-001 ด้วย และไม่จำกัดแค่ Status=Failed — เปลี่ยนไปเช็ค `classification.Recommendation=="MaintenanceRequired"` แทน ให้ตรงกับ classifier จริง; (3) **Job idempotency guard throw ไม่ถูกจับ** — race ระหว่าง 2 คำขอ complete execution ซ้อนกันทำให้ `job.Complete()` (guard ที่เพิ่มตอน AUT-TEST-004) throw หลุดออกไปเป็น unhandled 500 แทนที่จะ idempotent เหมือนที่ตั้งใจ — เพิ่ม try/catch คืนสถานะปัจจุบันแบบ graceful; (4) **`AutomationObjectVerification.Complete` ไม่มี idempotency guard** — ไม่สม่ำเสมอกับ Execution/Job ที่เพิ่ง guard ไปใน PR เดียวกัน เพิ่ม guard + เช็คใน `ReportVerificationResultAsync` ให้ idempotent เหมือนกัน; (5) **`ClaimVerificationBatchAsync` ไม่มี Serializable transaction** — ต่างจาก `ClaimNextJobAsync` ที่ทำไว้แล้ว ทำให้ 2 agent claim verification เดียวกันซ้อนกันได้ — เพิ่ม transaction pattern เดียวกัน; (6) **`DbProfile.ConnectTimeoutSeconds` ไม่มี lower bound** — `Math.Min(ActionTimeoutSeconds, 5)` ได้ 0 ถ้า ActionTimeoutSeconds=0 ซึ่ง provider ตีความ 0 ว่า "รอไม่จำกัดเวลา" ตรงข้ามกับที่ AUT-P0-013 ตั้งใจแก้ — เปลี่ยนเป็น `Math.Clamp(ActionTimeoutSeconds, 1, 5)`; (7) **Suite query ไม่กรอง `IsDeleted`** — `ListSuitesAsync`/`GetSuiteAsync` ใหม่ไม่ได้กรอง soft-deleted case ต่างจาก query อื่นทั้งหมดในไฟล์เดียวกัน เพิ่ม `!IsDeleted` ครบ; เพิ่มเทสต์ใหม่ยืนยันทุกจุด: `AutomationCodeReviewFixTests.cs` (QA Hub, 6 tests) + `DatabaseValidatorTests.cs` เพิ่ม 1 test (agent); ผลตรวจสอบ QA Hub 194/194 ผ่าน (เพิ่มจาก 188), agent 50/50 ผ่าน (เพิ่มจาก 48); **ขอบเขตที่ตั้งใจไม่แก้ (deferred เป็น backlog ใหม่/บันทึกไว้แทน):** ดู AUT-P0-015/016 และ progress log ด้านล่างสำหรับ finding ที่เหลือ (architecture duplication, performance N+1, edge case ต่างๆ) (2026-08-27) |
| AUT-P0-015 | Automation Suite ซ้ำ pattern กับ TestSuite ทั้ง backend/frontend | TODO | - | พบระหว่าง code review PR #1 (2026-08-27): `AutomationSuite`/`AutomationSuiteCase`/`AutomationSuiteService`/`AutomationSuitesController` (AUT-P1-001/002) และ `AutomationSuiteTab` ใน frontend สร้างซ้ำ pattern เดียวกับ `TestSuite`/`TestSuiteCase`/`TestSuiteService`/`TestSuitesPage` ที่มีอยู่แล้วแบบ field-for-field ไม่มี shared base เลย (รวมถึงใช้ชื่อ record `SuiteCaseDto` ซ้ำกันคนละ namespace) — ทั้งสองระบบมี field ต่างกันแล้ว (TestSuite มี SuiteType/RiskTier/NameExistsAsync/hard-delete ที่ AutomationSuite ไม่มี) ในอนาคตแก้ bug/เพิ่มฟีเจอร์ (reorder แบบ atomic, drag-drop, cascade-delete) ต้องทำซ้ำสองที่ ควรพิจารณาสกัด shared abstraction (entity/repository/service generic หรือ UI component/hook ร่วม) แต่เป็นงาน refactor ใหญ่ที่กระทบทั้งสองระบบ ควรวางแผนแยกต่างหาก |
| AUT-P0-016 | RetrySafety "Conditional" ความหมายไม่ชัดเจน ต้องตัดสินใจ product | TODO | - | พบระหว่าง code review PR #1 (2026-08-27): `GetUnsafeActionCodesAsync` ปฏิบัติกับ `RetrySafety=="Conditional"` เหมือน `"Unsafe"` ทุกประการ (`RetrySafety != "Safe"` คือ unsafe) แต่ frontend (Action Library) อธิบาย Conditional ว่า "retry ถ้ายังไม่สำเร็จ" ซึ่งสื่อว่าบางกรณีควร retry ได้ ผลคือ action ที่ mark เป็น Conditional จะบล็อก retry เสมอเหมือน Unsafe จริงๆ ทำให้ฟีเจอร์ที่ UI โฆษณาไว้ใช้งานไม่ได้จริง ต้องให้ทีมตัดสินใจความหมายที่แท้จริงของ Conditional ก่อนแก้โค้ด (เช่น ต้องเช็ค action result เพิ่มไหมว่า "ยังไม่สำเร็จ" หมายถึงอะไร) |

## 4. P0 — Automated Tests

| ID | งาน | สถานะ | Owner | Acceptance Criteria / หลักฐาน |
|---|---|---|---|---|
| AUT-TEST-001 | DSL และ Validator tests | DONE | Claude | `AutomationDslValidatorTests.cs` 17 tests ครอบคลุม DslVersion/AutomationType required, unsupported version, empty/duplicate/non-contiguous/non-positive step no, unknown Action/Object/TestData (ทั้งกรณี library ว่างและไม่ว่าง) (2026-08-27) |
| AUT-TEST-002 | Automation Case workflow tests | DONE | Claude | `AutomationCaseWorkflowTests.cs` + `AutomationTestFixtures.cs` (shared InMemory seed helper) ครอบคลุม Draft → version → Validate → Approve → Ready, MaintenanceRequired → AssignOwner → Resolve → NeedsReview, non-candidate TestCase reject, invalid DSL → version Invalid + case NeedsReview, resolve-maintenance guard เมื่อไม่ได้อยู่ MaintenanceRequired (2026-08-27) |
| AUT-TEST-003 | Atomic Job Claim tests | DONE | Claude | `AutomationJobClaimTests.cs` 7 tests: claim ที่ 2 ไม่ได้ job เดิมซ้ำ, claim สำเร็จ assign job+agent ถูกต้องและ execution เริ่ม Running, agent ปิดใช้งาน claim ไม่ได้, ไม่มี job คิว claim ได้ null, job ที่ QueuedAt อยู่อนาคต (backoff) claim ไม่ได้ยัง, priority ต่ำกว่าถูก claim ก่อน, domain guard `AutomationJob.Assign` throw เมื่อ job ไม่ใช่ Queued แล้ว; **หมายเหตุ:** โค้ดจริงไม่มี field "lease token" แยกต่างหาก — ความ atomic มาจาก domain guard นี้ + Serializable transaction ใน `ClaimNextJobAsync` (relational provider เท่านั้น); EF InMemory ไม่รองรับ transaction จึงเทสต์ครอบคลุมเฉพาะ sequential invariant ไม่ใช่ concurrent race จริง — ต้องทำ integration test บน SQL Server/SQLite ถ้าต้องการยืนยัน race condition จริง (2026-08-27) |
| AUT-TEST-004 | Cancel/Timeout/Lease Recovery tests | DONE | Claude | `AutomationCancelTimeoutTests.cs` 9 tests: cancel Queued/Running (execution+job→Cancelled, case กลับ Ready), cancel execution ที่ terminal แล้วต้อง throw 409, Timeout/AgentLost complete ถูก classify, **late/duplicate result ไม่ทับสถานะเดิม** (agent ส่งผลช้าหลัง cancel หรือส่งซ้ำ) — แก้ bug จริงที่พบระหว่างเขียนเทสต์: เพิ่ม guard ใน `AutomationExecution.Complete`/`AutomationJob.Complete` ให้ throw ถ้าสถานะ terminal อยู่แล้ว และ `CompleteExecutionAsync` เช็คก่อนแล้ว return สถานะปัจจุบันแบบ idempotent แทนที่จะ overwrite; **หมายเหตุ:** ยังไม่มี background lease-expiry watchdog (ไม่มี hosted service ตรวจ stale Running job จาก heartbeat หมดอายุแล้ว mark AgentLost อัตโนมัติ) — ส่วน "expired lease" ใน AC เดิมยังไม่ implement จริง แนะนำเปิดเป็นงานใหม่ (AUT-P1 หรือ P2) ถ้าต้องการ auto-recovery จริง (2026-08-27) |
| AUT-TEST-005 | Retry tests | DONE | Claude | `AutomationRetryTests.cs` 7 tests: retryable failure สร้าง retry execution เดียว (RetryOfExecutionId/RetryCount ถูก, backoff ทำให้ claim ทันทีไม่ได้), retry หยุดเมื่อ RetryCount ถึง MaxAttempts (ไล่ retry 2 รอบด้วย backoff=0 แล้วยืนยันไม่มีรอบที่ 3), non-retryable UI failure (AUT-UI-001) ไม่ retry และเข้า MaintenanceRequired, error code ที่ไม่รู้จักไม่ retry case กลับ Ready, retry policy Disabled ไม่สร้าง retry เลย, unsafe action ที่ Pass ไปแล้วบล็อก retry แม้ classification บอกว่า retryable, duplicate completion report ไม่สร้าง retry ซ้ำ (2026-08-27) |
| AUT-TEST-006 | Batch Run/Multi-Agent tests | DONE | Claude | `AutomationBatchRunTests.cs` 7 tests: batch สร้าง execution ต่อ 1 Ready case และ skip case ที่ยังไม่ approve, dedup case id ซ้ำ, request ว่างเปล่า throw, 2 agent ไล่ claim คิวเดียวกันไม่ชนกันและผลรวมครบ (4/4), target routing กรณี non-WindowsUI poller ถูก, non-retryable target mismatch, requested-agent ไม่ถูกบังคับจริง; **หมายเหตุ:** พบว่า target-based routing ทำงานทางเดียวเท่านั้น — poller ที่ใช้ targetApp="WindowsUI" (default) ไม่มี filter เลย เคลม job ของ AutomationType ไหนก็ได้ (ตรงข้ามกับที่คาดตอนแรก), ไม่มี capability-based routing จริง (`capabilities` param ถูกรับแต่ไม่ใช้กรองเลย), และ `AutomationJob.RequestedAgentId` (จาก `BatchRunRequest.AgentId`) ไม่ถูกบังคับตอน claim — agent ไหนก็แย่ง job ที่ "จอง" ให้ agent อื่นได้ ส่วน AC เดิม "กระจายงานตาม capability/target" จึงยังไม่ตรงกับพฤติกรรมจริงทั้งหมด แนะนำเปิดเป็นงานปรับปรุงแยกถ้าต้องการ routing จริง (2026-08-27) |
| AUT-TEST-007 | Evidence security tests | DONE | Claude | `AutomationEvidenceSecurityTests.cs` 8 tests: type/size reject (step และ execution evidence upload), path-traversal ผ่าน `evidenceType` ที่ฝัง `..` ถูกบล็อกไม่ให้หลุด evidence root, upload สำหรับ step/execution ที่ไม่มีจริงคืน NotFound และลบไฟล์ orphan ที่เขียนไปก่อนหน้า, upload สำเร็จเก็บไฟล์เฉพาะโฟลเดอร์ execution ตัวเอง, `GetEvidencePathAsync` ถูก scope ตาม projectId (project อื่นเห็นไม่ได้แม้รู้ executionId/stepResultId); ทดสอบโดย instantiate `AutomationAgentController` ตรงๆ (ไม่ต้องใช้ WebApplicationFactory) เพราะ method ไม่แตะ HttpContext; **หมายเหตุขอบเขต:** `[Authorize(Policy="AutomationEvidence")]` และ `[RequireProjectAccess]` filter บน `AutomationController` เป็น ASP.NET pipeline-level ต้องมี hosted server ถึงทดสอบได้จริง — ส่วนนี้ยกให้ AUT-TEST-008 (Permission tests) แทนที่จะ fake ผ่านไป (2026-08-27) |
| AUT-TEST-008 | Automation permission tests | DONE | Claude | `AutomationAuthorizationPolicyTests.cs` 25 tests ครอบคลุมทั้ง 8 policy (View/Edit/Validate/Approve/Execute/Manage/Evidence/GenerateAI): ผ่านเมื่อมี claim ที่ตรงเป๊ะ, fail เมื่อไม่มี claim เลย, fail เมื่อมี Automation permission อื่นที่ไม่ตรง (กัน cross-privilege escalation), AutomationExecute ผ่านได้ 3 ทาง (AUTOMATION.EXECUTE claim/EXECUTION.RUN claim/SYS_ADMIN role) และ fail เมื่อไม่มีทั้งสาม; รีแฟกเตอร์ `Program.cs` แยก policy registration ออกเป็น `AutomationAuthorizationPolicies.AddAutomationPolicies()` extension (`src/ProMaxx2.QA.Api/Services/`) ให้เทสต์เรียกใช้ policy set เดียวกันกับที่ API ใช้จริงผ่าน `AddAuthorizationCore` บน `ServiceCollection` เปล่าๆ (ไม่ต้องพึ่ง WebApplicationFactory/Mvc.Testing); **ขอบเขตที่ไม่ครอบคลุม (ตั้งใจ):** ไม่ได้ทดสอบว่า endpoint ไหนผูกกับ policy ไหนจริง (controller wiring) และ `[RequireProjectAccess]` filter — ทั้งสองต้องมี hosted pipeline จริง (2026-08-27) |
| AUT-TEST-009 | Agent ActionExecutor tests | DONE | Claude | สร้าง test project ใหม่ `agent/ProMaxx2.Automation.Core.Tests` (เพิ่มเข้า `ProMaxx2.Automation.slnx`) พร้อม `FakeUiAutomationDriver` (fake `IUiAutomationDriver`, ไม่พึ่ง FlaUI/Windows UI Automation จริง); `ActionExecutorTests.cs` 18 tests ครอบคลุม UI action (CLICK/LOGIN) สำเร็จ, unresolved object → AUT-UI-001, driver คืน false (จำลอง timeout แบบ "condition not met") → AUT-UI-003, driver throw `TimeoutException` → fallback AUT-UI-003, cancellation ก่อน dispatch → "Execution cancelled." + screenshot evidence, unsupported action → fallback, assertion actions (EXPECT_TEXT/EXPECT_VISIBLE/EXPECT_NOT_VISIBLE), DB assertion ไม่มี DB config → AUT-DB-001, evidence capture เฉพาะตอน fail; **แก้ bug จริงที่พบระหว่างเขียนเทสต์:** `ActionExecutor.ExecuteAsync` catch(Exception) เดิม hardcode `ErrorCode="AUT-UI-003"` เสมอ ทั้งที่ exception message ฝัง error code เฉพาะไว้แล้ว (เช่น "...not found in Object Repository (AUT-UI-001).", "...not configured (AUT-DB-001).") — เพิ่ม `ExtractErrorCode` ดึงโค้ดจาก pattern `(AUT-XXX-NNN)` ท้ายข้อความ ใช้แทน hardcode (fallback AUT-UI-003 เมื่อไม่มีโค้ดฝังอยู่); **หมายเหตุสำคัญ — พบ gap ใหญ่กว่าที่ยังไม่แก้:** `agent/ProMaxx2.Automation.Runner/Program.cs:285` เมื่อ execution fail จะ hardcode execution-level ErrorCode เป็น `"AUT-UI-003"`/FailureType `"AssertionFailure"` เสมอ **โดยไม่สนใจ ErrorCode จริงของ step ที่ fail** (แม้ ActionExecutor จะรายงานโค้ดที่ถูกต้องระดับ step แล้วก็ตาม) ผลคือ server-side `AutomationFailureClassifier` (ที่ทดสอบไว้ใน AUT-TEST-005) แทบไม่มีทางเห็นโค้ด AUT-DB-*/AUT-APP-*/AUT-AGENT-* จาก execution จริงเลย ทำให้ recommendation "Retry"/"RetryOrCheckEnvironment" แทบไม่ถูกใช้งานจริงในสนาม (เข้าถึงได้แค่ผ่าน fatal-exception path กับ DSL-empty case เท่านั้น) — ยังไม่แก้เพราะเป็นการเปลี่ยนพฤติกรรม retry ในสนามจริงที่ควรให้ทีมตัดสินใจก่อน แนะนำเปิดเป็นงานแยก (2026-08-27) |
| AUT-TEST-010 | Database Validator tests | DONE | Claude | เพิ่ม `DbAssertionComparer` (`agent/ProMaxx2.Automation.Core/DbAssertion.cs`) แยก Compare operator logic (`>=`/`<=`/`!=`/`>`/`<`/`=`/numeric/bool/string) และ parameter-name normalization ออกจาก `FirebirdDbValidator` (เดิมเป็น private method เข้าถึงไม่ได้จนกว่าจะต่อ DB จริงสำเร็จ) มาเป็น shared static class ที่ทั้ง Firebird และ SQL Server validator เรียกใช้ร่วมกัน — **แก้ inconsistency จริงที่พบ: `SqlServerDbValidator` เดิมไม่รองรับ operator เลย (ใช้ case-insensitive string equals ตรงๆ) ต่างจาก Firebird ที่รองรับครบ** ตอนนี้ทั้งสองใช้ logic เดียวกัน; `DatabaseValidatorTests.cs` 45 tests ครอบคลุม Compare ทุก operator + numeric/bool/string fallback, parameter name normalization, `DbValidatorFactory.Create` คืน type ถูกต้องตาม Firebird/SqlServer, และ Firebird validator ต่อ connection ที่ถูกปฏิเสธ (127.0.0.1 port ปิด) จริงคืน failure result ที่ถูกต้อง (เร็ว ไม่ต้องมี DB จริง); **ขอบเขตที่ไม่ครอบคลุม (ตั้งใจ):** ไม่มี live-connection test สำหรับ SQL Server เพราะ `Microsoft.Data.SqlClient` ใช้เวลา ~14 วินาทีกว่าจะ fail connection ที่ถูกปฏิเสธ (validator ไม่ได้ set `ConnectTimeout` เลย ใช้ default 15s) ทำให้ suite ช้าลงมากสำหรับ assertion เดียว — เปิดเป็นงานใหม่ `AUT-P0-013`; parameter binding/query execution จริงกับ query failure ระดับ query (ไม่ใช่ connection) ต้องมี DB จริงถึงทดสอบได้ ยังไม่ครอบคลุม; **หมายเหตุสำคัญอีกจุด:** `ValidateAsync(request, ct)` รับ `CancellationToken` มาแต่ไม่เคยใช้เลย (`con.Open()`/`cmd.ExecuteReader()` เป็น sync call ไม่ใช่ `OpenAsync(ct)`/`ExecuteReaderAsync(ct)`) แปลว่า DB assertion step ที่ query ค้างจะ cancel ไม่ได้จริง — รวมอยู่ใน `AUT-P0-013` เช่นกัน (2026-08-27) |

## 5. P1 — Automation Suite และ Scheduling

| ID | งาน | สถานะ | Owner | Acceptance Criteria / หลักฐาน |
|---|---|---|---|---|
| AUT-P1-001 | Persistent Automation Suite | DONE | Claude | Entity `AutomationSuite` (domain) + migration `AddAutomationSuites` (apply สำเร็จบน dev DB จริง) — SuiteCode/SuiteName/Description/IsActive/Created*/Updated*/Closed* พร้อม unique index (ProjectId, SuiteCode); `AutomationSuiteService`/`IAutomationSuiteRepository` (Create/Update/Close/Reopen/List/Get, auto-gen code จาก `{ProjectCode}-AS-NNN` ถ้าไม่ระบุ, project-scoped); endpoint `AutomationSuitesController` (`GET/POST api/v1/automation/suites`, `GET/PUT {id}`, `POST {id}/close`, `POST {id}/reopen`) ใช้ policy `AutomationView`/`AutomationEdit` เดิม; frontend tab ใหม่ "Automation Suite" (list + create/edit modal + close/reopen) — ดูหลักฐานรวมกับ AUT-P1-002 ด้านล่าง (2026-08-27) |
| AUT-P1-002 | จัดการ Case ใน Suite | DONE | Claude | Entity `AutomationSuiteCase` (join table, SortOrder/IsRequired) + migration `AddAutomationSuiteCases` (FK ไป AutomationSuites Cascade / AutomationCases Restrict, apply สำเร็จบน dev DB จริง); service เพิ่ม `AddCasesAsync`/`UpdateCaseAsync`/`RemoveCaseAsync` — ตรวจ case ต้องอยู่ project เดียวกับ suite (ArgumentException ถ้าไม่ใช่), add ซ้ำ idempotent (skip ไม่ error), sort order auto-increment, บล็อกแก้ไข case บน suite ที่ปิดแล้วทุก action (ต้อง reopen ก่อน); `AutomationSuiteDto`/`AutomationSuiteListDto` ส่ง Target(AutomationType)/Status ของแต่ละ case กลับมาด้วย (ตรง AC "ตรวจ Target/Status ได้"), `ListDto` มี CaseCount/ReadyCaseCount แยกกัน; endpoint เพิ่ม `POST/PUT/DELETE {id}/cases[/{caseId}]`; frontend: detail modal ในหน้า Automation Suite แสดงรายการ case พร้อมปุ่มเลื่อนลำดับ (↑↓ สลับ sortOrder), toggle Required/Optional, ลบ case, modal เพิ่ม case (multi-select จาก case ที่ยังไม่อยู่ใน suite); เทสต์รวม `AutomationSuiteTests.cs` (11) + `AutomationSuiteCaseTests.cs` (11) = 22 tests ครอบคลุมทั้ง P1-001/002; QA Hub `dotnet build` 0 warning/0 error, `dotnet test` 188/188 ผ่าน (เพิ่มจาก 166), frontend `npm run build`/`npm run lint` ผ่าน, `git diff --check` ผ่าน (2026-08-27) |
| AUT-P1-003 | Suite Version/History | DONE | Claude | Entity `AutomationSuiteRevision` ใหม่ (audit-trail entry: RevisionNo/ChangeType/Detail/ChangeReason/ChangedBy/ChangedAt) + `AutomationSuite.RevisionNo` counter (`BumpRevision()`) + migration `AddAutomationSuiteRevisions` (เพิ่มคอลัมน์ RevisionNo บน AutomationSuites + backfill แถวเดิมเป็น 1 ด้วย SQL, สร้างตาราง AutomationSuiteRevisions ใหม่ — apply สำเร็จบน dev DB จริง); ทุก mutation ของ suite (Update/Close/Reopen) และ case membership (AddCases/UpdateCase/RemoveCase) บันทึก revision อัตโนมัติพร้อม Detail อัตโนมัติ (เช่น "Renamed X → Y", "Added 2 case(s): CODE1, CODE2") และรับ `ChangeReason` ไม่บังคับจาก caller; AddCases แบบ idempotent (case ที่มีอยู่แล้วทั้งหมด) ไม่สร้าง revision ปลอม; endpoint ใหม่ `GET {id}/history`; frontend: ปุ่ม "ประวัติ" ทั้งจาก row action และใน suite detail modal เปิด modal แสดงรายการ revision (ล่าสุดก่อน) พร้อม Detail/เหตุผล/ผู้แก้ (resolve ชื่อจาก Users)/เวลา, เพิ่มช่อง "เหตุผลที่แก้ไข" (ไม่บังคับ) ใน edit modal และ add-cases modal, แสดง RevisionNo ปัจจุบันใน suite detail header; `AutomationSuiteHistoryTests.cs` 7 tests ครอบคลุม Created revision, Update บันทึก reason+detail, Close/Reopen แยก revision, AddCases รวม case code ใน detail, idempotent add ไม่สร้าง revision ปลอม, UpdateCase/RemoveCase ระบุชื่อ case, history project-scoped; QA Hub `dotnet build` 0 warning/0 error, `dotnet test` 201/201 ผ่าน (เพิ่มจาก 194); frontend `npm run build`/`npm run lint` ผ่าน, `git diff --check` ผ่าน (2026-08-27) |
| AUT-P1-004 | Run Suite ซ้ำ | DONE | Claude | `AutomationAgentService.RunSuiteAsync` ใหม่ (`RunSuiteRequest(AutomationSuiteId, BuildId, EnvironmentId, AgentId, Priority)`) — ดึง case membership ปัจจุบันของ Suite ทั้งหมดแล้วเรียก `BatchRunAsync` เดิมต่อ (ใช้ Ready/Quarantine filter, version resolution, skip-reporting ชุดเดียวกับ Batch Run manual ทุกอย่าง ไม่ต้องเขียนใหม่); guard: suite ปิดอยู่/ไม่มี case เลย throw ตามลำดับ; endpoint `POST {id}/run` (policy `AutomationExecute` — คนละ policy จาก endpoint จัดการ suite อื่นๆ ที่ใช้ `AutomationEdit`) รับ body แยก `RunSuiteBodyRequest` (ไม่มี suiteId ซ้ำเพราะมาจาก route แล้ว); frontend: ปุ่ม "▶ รัน" ทั้งจาก row action และ suite detail modal เปิด `RunSuiteModal` (เลือก Build/Environment/Priority แบบเดียวกับ Batch Run modal เดิม แต่ไม่มีการเลือก Case เพราะใช้ชุดเดิมของ Suite) แสดงผลลัพธ์ created/skipped หลังสั่งรัน; `AutomationSuiteRunTests.cs` 6 tests ครอบคลุม run สำเร็จไม่ต้องเลือก case ใหม่, รันซ้ำ 2 ครั้งกับ build ต่างกันโดยไม่ต้อง reselect case, skip case ที่ไม่ Ready, ปิด suite แล้วรันไม่ได้, suite ไม่มี case รันไม่ได้, project scoping; QA Hub `dotnet build` 0 warning/0 error, `dotnet test` 207/207 ผ่าน (เพิ่มจาก 201); frontend build+lint ผ่าน (2026-08-27) |
| AUT-P1-005 | Persistent Automation Schedule | DONE | Claude | Entity `AutomationSchedule` ใหม่ (Frequency Once/Daily/Weekly, DaysOfWeekMask bitmask, RunAtTime/OnceOnDate, TimeZoneId, target Build/Environment/Agent/Priority, IsActive, NextRunAtUtc คำนวณเอง) + migration `AddAutomationSchedules` (ตาราง `AutomationSuiteSchedules` — ตั้งชื่อเลี่ยง `AutomationSchedules` เพราะชื่อนั้นถูกใช้อยู่แล้วโดยตาราง orphan จาก Automation module เวอร์ชันเก่าที่ไม่มี entity ใดแม็พอยู่บน dev DB จริง ป้องกัน DDL ชนกัน — apply สำเร็จบน dev DB จริง); คำนวณ NextRunAtUtc ด้วย `TimeZoneInfo` (แปลง wall-clock ใน timezone ที่เลือกเป็น UTC ให้ถูกต้องข้าม DST) รองรับ Once (วันที่เดียว ต้องเป็นอนาคต)/Daily (เวลาเดียวกันทุกวัน)/Weekly (สแกนหาไปข้างหน้า 8 วันจนเจอวันที่เลือกไว้); `AutomationScheduleService`/`IAutomationScheduleRepository` (Create/Update/Activate/Deactivate/List/Get, ตรวจ Suite ต้องมีอยู่จริงใน project ก่อนผูก schedule); endpoint `AutomationSchedulesController` (`GET/POST api/v1/automation/schedules`, `GET/PUT {id}`, `POST {id}/activate`, `POST {id}/deactivate`) ใช้ policy `AutomationView`/`AutomationEdit` เดิม; frontend tab ใหม่ "Schedule" — list พร้อมคำอธิบายตารางเวลาแบบอ่านง่าย (เช่น "ทุกสัปดาห์ (จ,พ,ศ) 09:00"), filter active/inactive, create/edit modal (เลือก Suite/Frequency/วันในสัปดาห์/เวลา/Timezone ผ่าน `Intl.supportedValuesOf` พร้อม fallback list/Build/Environment/Agent/Priority), ปุ่มเปิด/ปิดใช้งาน; หมายเหตุ scope: ยังไม่มี worker ที่ยิงรันจริงตามเวลา (นั่นคือ AUT-P1-006 ที่แยกไว้ต่างหาก) — งานนี้ส่งมอบแค่ config CRUD + คำนวณ next-run เท่านั้น ตรงตาม AC "สร้าง/แก้ไข/เปิด/ปิด Schedule พร้อม timezone และ next run"; `AutomationScheduleTests.cs` 13 tests ครอบคลุม Daily/Weekly/Once คำนวณ next-run ถูกต้อง, Weekly ไม่เลือกวันเลย throw, Once ในอดีต throw, timezone ผิดพลาด throw, แปลงเวลาข้าม timezone จริง (Bangkok UTC+7), suite ไม่มีอยู่จริง throw, Update คำนวณ next-run ใหม่, Activate/Deactivate สลับสถานะและ guard ซ้ำ, List filter isActive; QA Hub `dotnet build` 0 warning/0 error, `dotnet test` **220/220 ผ่าน** (เพิ่มจาก 207); frontend `npm run build` (tsc+vite)/`npm run lint` (oxlint) ผ่านทั้งคู่, `git diff --check` ผ่าน (2026-08-27) |
| AUT-P1-006 | Schedule Execution Worker | DONE | Claude | `AutomationScheduleWorker` (`BackgroundService` ใหม่ — ตัวแรกในทั้ง solution, poll ทุก 30s) เรียก `AutomationAgentService.FireDueSchedulesAsync(nowUtc, ct)` ทุก tick; core logic ทั้งหมดอยู่ที่ Application layer (ทดสอบได้โดยไม่ต้องพึ่ง timer จริง): `IAutomationScheduleRepository.ClaimDueSchedulesAsync` claim schedule ที่ถึงเวลาแบบ atomic ด้วย Serializable transaction (แพทเทิร์นเดียวกับ `ClaimNextJobAsync`/`ClaimVerificationBatchAsync`) — SELECT `IsActive && NextRunAtUtc<=now` แล้วเรียก `AutomationSchedule.RecordFired(nowUtc)` ทันที (advance NextRunAtUtc ไปรอบถัดไปสำหรับ Daily/Weekly หรือ deactivate สำหรับ Once) ก่อน commit ในธุรกรรมเดียว **ทำให้ due instant ถูก "ใช้" ทันทีที่ claim ป้องกัน tick ซ้อนกัน 2 ครั้งยิงซ้ำ (ตรง AC "สร้าง Job ครั้งเดียวตามเวลา")**; หลัง claim แล้วค่อยเรียก `RunSuiteAsync` จริงแยกออกมา (claim-then-act เหมือน `ClaimNextJobAsync`) กันปัญหาถ้า run ล้มเหลวจะไม่ทำให้ schedule ค้าง retry ทุก tick; **"recovery หลัง restart" ได้มาฟรีโดยไม่ต้องเขียน logic เพิ่ม** — NextRunAtUtc ขยับไปข้างหน้าเฉพาะตอน claim สำเร็จเท่านั้น ถ้า app ปิดอยู่ตอนถึงเวลาที่ควรรัน เวลานั้นจะยังเป็นอดีตอยู่จนกว่า tick แรกหลัง restart จะ claim ได้ (ไม่มี logic แยกสำหรับ "missed schedule" เพราะไม่จำเป็น); Daily/Weekly คำนวณรอบถัดไปจาก `nowUtc` จริงตอน fire (ไม่ใช่ due time เดิม) ทำให้ตามทันครั้งเดียวไม่สร้างคิวงานที่พลาดสะสมย้อนหลัง; entity ใหม่ `AutomationScheduleRun` (audit: FiredAtUtc/Status Succeeded-NoReadyCases-Failed/ExecutionsCreated/SkippedCount/ErrorMessage) บันทึกทุกครั้งไม่ว่าผลจะเป็นอย่างไร **ตรง AC "audit ผล"**; `AutomationSchedule` เพิ่ม `LastRunAtUtc` + `RecordFired()`; migration `AddAutomationScheduleRuns` (คอลัมน์ LastRunAtUtc บน AutomationSuiteSchedules + ตาราง AutomationScheduleRuns ใหม่ — apply สำเร็จบน dev DB จริง); endpoint ใหม่ `GET {id}/runs` (policy `AutomationView`) คืนประวัติเรียงล่าสุดก่อน; frontend: คอลัมน์ "รันล่าสุด" ในตาราง schedule + ปุ่ม "ประวัติการรัน" เปิด modal แสดง Status (Badge สี)/เวลา/จำนวน Execution ที่สร้าง/ข้อความ error; **ขอบเขตที่ตั้งใจไม่ทำ:** ไม่มี config ปรับ poll interval ได้ (hardcode 30s — เป็น background service ตัวแรกในระบบ ยังไม่มี config convention ให้ตาม), ไม่ทดสอบ `AutomationScheduleWorker`/timer loop ตรงๆ (เทสต์ core logic ผ่าน `FireDueSchedulesAsync` แทนซึ่งครอบคลุม business logic ทั้งหมดจริง ส่วน BackgroundService เป็นแค่ timer wrapper บางๆ); `AutomationScheduleWorkerTests.cs` 8 tests ครอบคลุม fire สำเร็จสร้าง execution + advance รอบถัดไป, Once fire แล้ว deactivate, fire ซ้ำ instant เดียวกันสร้างแค่ 1 execution (exactly-once), schedule ยังไม่ถึงเวลาไม่ถูกยิง, schedule ปิดอยู่ไม่ถูกยิงแม้ NextRunAtUtc ผ่านไปแล้ว, suite ไม่มี case Ready เลย → audit NoReadyCases แต่ schedule ยัง advance ต่อ, suite ว่างเปล่าทั้งหมด (0 case) → audit Failed แต่ schedule ยัง advance ต่อ (ไม่ค้าง retry), list ประวัติของ schedule ที่ไม่มีอยู่จริง throw; QA Hub `dotnet build` 0 warning/0 error, `dotnet test` **228/228 ผ่าน** (เพิ่มจาก 220); frontend `npm run build`/`npm run lint` ผ่านทั้งคู่, `git diff --check` ผ่าน (2026-08-27) |
| AUT-P1-007 | Build Trigger | DONE | Claude | Entity `AutomationBuildTriggerPolicy` ใหม่ (ProjectId/AutomationSuiteId/Pack Smoke-Regression/EnvironmentId/AgentId/Priority/IsActive) — **ตัดสินใจแยกความหมาย Pack ให้มีผลจริงต่อจังหวะที่ trigger** เพราะ `CreateBuildRequest` เดิมไม่มีฟิลด์ IsReleaseCandidate (ตั้งแยกทีหลังผ่าน `MarkRcAsync`) ทำให้ filter "เฉพาะ RC" ที่จุดสร้าง build ใช้ไม่ได้จริง จึงผูก **Smoke → trigger ทุกครั้งที่สร้าง Build ใหม่ (`ReleaseService.CreateBuildAsync`)**, **Regression → trigger เฉพาะตอน Build ถูกตั้งเป็น Release Candidate (`ReleaseService.MarkRcAsync`)** ตรงกับพฤติกรรมจริงที่ทีมส่วนใหญ่ต้องการ (Smoke เร็วรันทุก build, Regression หนักกว่ารันก่อนขึ้น release); `AutomationBuildTriggerService.FireForBuildAsync(projectId,buildId,pack,ct)` หา active policy ตรง pack แล้วเรียก `AutomationAgentService.RunSuiteAsync` เดิม — **best-effort เสมอ:** catch exception กว้างแล้วบันทึกเป็น audit `AutomationBuildTriggerRun` (Succeeded/NoReadyCases/Failed) แทนการโยนขึ้นไป เพื่อไม่ให้ policy พัง (suite ปิด/ถูกลบ) ทำให้ "สร้าง Build" หรือ "Mark RC" ที่เป็น action หลักของผู้ใช้ล้มเหลวไปด้วย; **สถาปัตยกรรม:** `ReleaseService` (namespace Releases) inject `AutomationBuildTriggerService` (namespace Automation) ตรงๆ — ทั้งสอง namespace อยู่ assembly เดียวกัน (`ProMaxx2.QA.Application`) จึงไม่มีปัญหา circular reference, มี precedent เดิมอยู่แล้วที่ Application service อ้างอิง Application service อื่นข้าม namespace ตรงๆ (เช่น `TestSummaryService(ReleaseService,DashboardService)`, `AutomationCaseService` อ้าง `ITestCaseRepository`); reuse `IAutomationSuiteRepository`/`AutomationAgentService.RunSuiteAsync` เดิมทั้งหมด ไม่เขียน execution logic ซ้ำ; migration `AddAutomationBuildTriggers` (ตาราง `AutomationBuildTriggerPolicies` + `AutomationBuildTriggerRuns` — apply สำเร็จบน dev DB จริง); endpoint `AutomationBuildTriggersController` (`GET/POST api/v1/automation/build-triggers`, `GET/PUT {id}`, `POST {id}/activate`, `POST {id}/deactivate`, `GET {id}/runs`) policy `AutomationView`/`AutomationEdit` เดิม; frontend tab ใหม่ "Build Trigger" — list Pack/Suite/Environment/Agent/Priority/สถานะ, create/edit modal, ปุ่มเปิด/ปิดใช้งาน, ประวัติการรัน modal (โครงเดียวกับ Schedule tab); test: `AutomationBuildTriggerTests.cs` ใหม่ 9 tests — **ทดสอบผ่าน `ReleaseService` จริง ไม่ใช่เรียก `FireForBuildAsync` ตรงๆ** เพื่อยืนยัน wiring จริงใน `CreateBuildAsync`/`MarkRcAsync` ครอบคลุม: สร้าง build trigger เฉพาะ Smoke policy, สร้าง build ไม่ trigger Regression policy, mark RC trigger ทั้ง Smoke(จากตอนสร้าง)+Regression(จากตอน mark RC)รวมเป็น 2 execution ไม่ซ้ำไม่ขาด, policy ปิดอยู่ไม่ trigger, suite ปิดอยู่ trigger แล้ว fail แต่ build ยังสร้างสำเร็จปกติ (audit Failed), suite ไม่มีอยู่จริงตอนสร้าง policy throw, pack ผิด (ไม่ใช่ Smoke/Regression) throw, list คืน policy ตาม project, activate policy ที่ active อยู่แล้ว throw; QA Hub `dotnet build` 0 warning/0 error, `dotnet test` **237/237 ผ่าน** (เพิ่มจาก 228); frontend `npm run build`/`npm run lint` ผ่านทั้งคู่, `git diff --check` ผ่าน (2026-08-27) |
| AUT-P1-008 | CI/CD หรือ Webhook Integration | DONE | Claude | Entity `AutomationWebhookToken` (secret แบบ random 256-bit, เก็บแค่ SHA-256 hash — plaintext คืนให้แค่ครั้งเดียวตอนสร้างแล้วไม่มีทางดึงกลับมาดูอีก, เก็บ `TokenPrefix` 12 ตัวแรกไว้แสดงใน UI แยกแยะ token) + `AutomationWebhookDelivery` (audit ทุกครั้งที่มีการเรียก webhook); migration `AddAutomationWebhooks` (ตาราง `AutomationWebhookTokens` unique index บน TokenHash + `AutomationWebhookDeliveries` — apply สำเร็จบน dev DB จริง) — **ครบทั้ง 3 ข้อใน AC:** (1) **"รับ trigger แบบ authenticated"** — endpoint สาธารณะ `POST /api/v1/webhooks/automation/builds` อยู่นอก `[Authorize]`/JWT pipeline ของแอปทั้งหมด (เพราะระบบ CI ไม่มี user session) แต่ authenticate เองด้วย header `X-Webhook-Token` → hash แล้วค้นหา token ที่ active — **token เองเป็นตัวกำหนด scope project ด้วย ไม่ต้องพึ่ง field projectId ที่ client ส่งมา** (ป้องกัน token โปรเจกต์หนึ่งถูกใช้ส่ง build เข้าโปรเจกต์อื่นแม้เดา releaseId ถูก); (2) **"ป้องกัน replay"** — ใช้ pattern idempotency-key: client ต้องส่ง `RequestId` มาด้วยทุกครั้ง (เช่น CI run ID) ถ้าเคยมี delivery ที่ **สำเร็จ** ด้วย RequestId เดียวกันมาก่อนในโปรเจกต์เดียวกัน จะคืนผลลัพธ์เดิมทันทีไม่สร้าง Build ซ้ำ — **ตัดสินใจสำคัญ:** delivery ที่ "Failed" ต้องไม่ block การ retry ด้วย RequestId เดียวกัน (เช่น ครั้งแรก releaseId ผิดเพราะ config CI ยังไม่อัปเดต, แก้แล้ว retry ด้วย RequestId เดิมต้องผ่านได้) — เจอ bug นี้ตอนออกแบบตอนแรกที่ตั้งใจใส่ DB unique index บน (ProjectId,RequestId) ตรงๆ ซึ่งจะบล็อก retry-after-failure ไปด้วย จึงเปลี่ยนมาเป็น non-unique index + application-level check เฉพาะ delivery ที่ Status="Created" เท่านั้น; (3) **"trace กลับ Build ได้"** — response ของ webhook และทุก audit row มี `BuildId` เสมอ, endpoint `GET /webhook-tokens/deliveries` ให้ดูประวัติทั้งหมดพร้อม BuildNumber; **reuse เต็มรูปแบบ:** `ReceiveBuildAsync` เรียก `ReleaseService.CreateBuildAsync` ตัวเดียวกับที่ UI ปกติเรียก — ทำให้ Build ที่สร้างผ่าน webhook ยิง AUT-P1-007 Smoke trigger policy อัตโนมัติเหมือน Build ที่สร้างเองทุกประการ โดยไม่มี code path แยก (พิสูจน์ด้วยเทสต์ end-to-end); Token management (`AutomationWebhookTokensController` — List/Create/Revoke/ดูประวัติ) เป็น endpoint ปกติที่ auth ด้วย JWT+policy `AutomationView`/`AutomationEdit` เหมือนเดิมทุกจุด แยกคนละ controller จาก webhook รับจริงเพื่อไม่ให้ auth model ปนกัน; frontend tab ใหม่ "Webhook" — list token (โชว์แค่ prefix)/ปุ่มเพิกถอน/modal แสดง plaintext token ครั้งเดียวตอนสร้างพร้อมคำเตือนคัดลอกเก็บไว้/ตารางประวัติการเรียก; test: `AutomationWebhookTests.cs` ใหม่ 11 tests — สร้าง token คืน plaintext ครั้งเดียว, token ถูกต้องสร้าง build สำเร็จ, token ผิด/ถูกเพิกถอนแล้วถูกปฏิเสธ, เพิกถอนซ้ำ throw, RequestId ซ้ำหลังสำเร็จคืนผลเดิมไม่สร้าง build ซ้ำ, delivery ที่ fail ไม่บล็อก retry ด้วย RequestId เดิม, build number ซ้ำจริง (RequestId ต่างกัน) throw DuplicateCodeException, ไม่ส่ง RequestId throw, webhook-created build ยิง Smoke policy เหมือน build ปกติ, ประวัติการเรียกแสดงชื่อ token+build number ถูกต้อง; QA Hub `dotnet build` 0 warning/0 error, `dotnet test` **248/248 ผ่าน** (เพิ่มจาก 237); frontend `npm run build`/`npm run lint` ผ่านทั้งคู่, `git diff --check` ผ่าน (2026-08-27) |
| AUT-P1-009 | Schedule Notifications | DONE | Claude | Entity `AutomationScheduleNotification` ใหม่ (ProjectId/AutomationScheduleId/**AutomationExecutionId บังคับเสมอ** เพื่อให้ทุกแถวมีลิงก์ Execution ตรง AC/EventType Started-Completed-Failed-NoAgent/Message/IsRead/ReadAtUtc) + migration `AddAutomationScheduleNotifications` (ตาราง `AutomationScheduleNotifications`, FK ไป `AutomationSuiteSchedules` Cascade — apply สำเร็จบน dev DB จริง); **จุดสร้าง notification 2 จุด reuse ของเดิมทั้งหมด ไม่มี event bus/hook framework ใหม่:** (1) `AutomationAgentService.FireDueSchedulesAsync` (P1-006 เดิม) หลัง `RunSuiteAsync` สำเร็จ — สร้าง "Started" ให้ทุก Execution ที่สร้างจริง (`result.Created`) แล้วเช็ค `repository.ListAgentsAsync` ถ้าไม่มี agent ที่ `IsEnabled=true` เลยสักตัว สร้าง "NoAgent" เพิ่มอีก 1 แถว (ผูกกับ Execution แรกที่สร้าง เพราะ AC ต้องการลิงก์ Execution เสมอ); (2) `CompleteExecutionAsync` (P0 เดิม) หลัง `execution.Complete(...)` เช็คว่า Execution นี้มี "Started" notification อยู่ไหม (`FindStartedNotificationByExecutionAsync` — ถ้ามีแปลว่า Execution นี้เกิดจาก schedule fire) ถ้ามีสร้าง "Completed" (Status=Passed) หรือ "Failed" (สถานะ terminal อื่นทั้งหมด) พร้อม error message ที่ error จริง; **ตัดสินใจสำคัญเรื่องขอบเขต NoAgent:** เช็คแค่ "มี agent ที่ enabled อยู่จริงไหม" ไม่เช็ค capability/target ตรงกับ case หรือไม่ เพราะโค้ดจริงของ `ClaimNextJobAsync` (ยืนยันจาก AUT-TEST-006) ไม่ได้กรอง job ตาม capability เลยจริงๆ (กรองแค่ TargetApp และเฉพาะตอนไม่ใช่ WindowsUI default) — เช็ค capability ในงานนี้จะให้ความแม่นยำเกินที่ระบบจริงมี; **ขอบเขตที่ตั้งใจไม่ทำ:** เช็ค NoAgent แค่ตอน schedule fire เท่านั้น (ไม่มี background watchdog ตรวจ Execution ที่ค้าง Queued นานเกินไปภายหลัง เพราะยังไม่มี lease-expiry watchdog ในระบบเลยตามที่บันทึกไว้ใน AUT-TEST-004), auto-retry execution ที่เกิดจาก `CompleteExecutionAsync` เอง (ไม่ใช่จาก schedule fire ตรงๆ) ไม่มี "Started" notification เป็นของตัวเอง จึงไม่มี Completed/Failed notification แยกตอน retry เสร็จ (ผลของ retry ยังดูได้ปกติผ่าน retry chain ในหน้า Execution Detail เดิม); endpoint ใหม่ 4 ตัวใน `AutomationSchedulesController` (`GET notifications`, `GET notifications/unread-count`, `POST notifications/{id}/read`, `POST notifications/mark-all-read`) ใช้ policy `AutomationView` เดิม (การอ่าน/mark-read notification ไม่ใช่การแก้ config อัตโนมัติ จึงไม่ต้องการ `AutomationEdit`); frontend: ปุ่ม "🔔 การแจ้งเตือน" พร้อม badge unread count ใน header ของ Schedule tab เดิม เปิด modal แสดงรายการ (Badge สีตาม EventType, ข้อความ, เวลา, ปุ่ม "ดู Execution" ดึง Execution จาก `GET executions/{id}` แล้วเปิด modal Execution Detail เดิมของหน้า Execution ตรง — ไม่สร้าง Execution viewer ใหม่ซ้ำ, ปุ่ม mark-read รายตัว/ทั้งหมด); test: `AutomationScheduleNotificationTests.cs` ใหม่ 9 tests ครอบคลุม fire ไม่มี agent สร้าง Started+NoAgent ผูก ExecutionId เดียวกัน, fire มี agent สร้างแค่ Started, complete Passed สร้าง Completed, complete Failed สร้าง Failed พร้อม error message, complete execution ที่ไม่ได้มาจาก schedule ไม่สร้าง notification เลย, late/duplicate completion report ไม่สร้าง notification ซ้ำ, mark-read ตัดออกจาก unread filter/unread count, mark-all-read ล้าง unread count เป็น 0, mark-read notification ของ project อื่น throw; QA Hub `dotnet build` 0 warning/0 error, `dotnet test` **257/257 ผ่าน** (เพิ่มจาก 248); frontend `npm run build` (tsc+vite)/`npm run lint` (oxlint) ผ่านทั้งคู่, `git diff --check` ผ่าน (2026-08-27) |

## 6. P1 — Test Data Management

| ID | งาน | สถานะ | Owner | Acceptance Criteria / หลักฐาน |
|---|---|---|---|---|
| AUT-DATA-001 | Database Snapshot | TODO | - | snapshot ก่อนรันพร้อม metadata ของ Environment/Build |
| AUT-DATA-002 | Database Restore | TODO | - | restore หลังรันหรือเมื่อ fail และยืนยัน checksum/ความพร้อมใช้งาน |
| AUT-DATA-003 | Seed Test Data | TODO | - | seed แบบ repeatable/idempotent และไม่เก็บ credential ใน DSL |
| AUT-DATA-004 | Cleanup Test Data | TODO | - | cleanup สำเร็จแม้ execution ถูก cancel หรือ Agent หาย |
| AUT-DATA-005 | Master Data Setup Flow | TODO | - | เตรียมสินค้า/ราคา/โปรโมชั่นก่อน POS scenario ผ่าน UI หรือ approved DB seed |
| AUT-DATA-006 | Environment Data Profile | TODO | - | แยก config/data source ตาม Environment โดย secret อยู่ใน secure store |

## 7. P1 — Regression Closed Loop

| ID | งาน | สถานะ | Owner | Acceptance Criteria / หลักฐาน |
|---|---|---|---|---|
| AUT-REG-001 | Regression Impact → Run List | TODO | - | นำ recommended case IDs มาสร้าง Automation run list โดยตรวจ Ready/Target |
| AUT-REG-002 | Scheduled Regression → Automation | TODO | - | notification/build event เริ่ม workflow ได้อัตโนมัติและไม่สร้างงานซ้ำ |
| AUT-REG-003 | Multi-Agent Result Merge | TODO | - | รวมผลจากหลาย Agent ใน Test Cycle เดียวและแสดง partial/complete |
| AUT-REG-004 | TestExecution Write-back Verification | TODO | - | step result/evidence/source=Automation ครบและ dashboard อัปเดตถูกต้อง |
| AUT-REG-005 | Defect Closed Loop | TODO | - | Product Fail สร้าง draft defect หลัง QA confirm พร้อม evidence และ classification |
| AUT-REG-006 | End-to-End Regression Test | TODO | - | Build → Impact → Run → Write-back → Defect ผ่านจริงอย่างน้อย 1 release |

## 8. P2 — Monitoring, UX และ Scalability

| ID | งาน | สถานะ | Owner | Acceptance Criteria / หลักฐาน |
|---|---|---|---|---|
| AUT-P2-001 | Server-side pagination | TODO | - | Cases/Jobs/Executions ใช้ page/size/filter/sort ฝั่ง server |
| AUT-P2-002 | Advanced execution filters | TODO | - | กรองวันที่, Build, Environment, Agent, Target และ Failure Type |
| AUT-P2-003 | Pass/Fail/Flaky trend | TODO | - | กราฟแนวโน้มตามวัน/Build/Release และ drill down ได้ |
| AUT-P2-004 | Agent workload/history | TODO | - | แสดง utilization, queue time, runtime, failure และ heartbeat history |
| AUT-P2-005 | Bulk Revalidate/Repair | TODO | - | เลือก MaintenanceRequired หลาย Case แล้ว revalidate พร้อมผลรายรายการ |
| AUT-P2-006 | Export execution report | TODO | - | Export CSV/Excel ตาม filter พร้อม summary และ trace IDs |
| AUT-P2-007 | Stuck Job/Offline Agent alert | TODO | - | แจ้งเมื่อ heartbeat หาย, lease หมด, queue เกิน SLA และกดไปแก้ได้ |
| AUT-P2-008 | Accessibility/Responsive QA | TODO | - | ตรวจ 1440/1024/768/390px, keyboard, focus, modal และไม่มี page-level horizontal scroll |

## 9. Documentation และมาตรฐาน

| ID | งาน | สถานะ | Owner | Acceptance Criteria / หลักฐาน |
|---|---|---|---|---|
| AUT-DOC-001 | รวม Automation Roadmap | TODO | - | เอกสาร Phase 0–5 และ G0–G10 ไม่ขัดกันและอ้าง tracker นี้ |
| AUT-DOC-002 | อนุมัติ Selector Contract | BLOCKED | QA + ProMaxx2 Dev | ทีม Dev อนุมัติ contract และมี owner สำหรับแก้ missing/duplicate ID |
| AUT-DOC-003 | ขยาย P0 Smoke Set | TODO | - | มี 10–15 Case ครบ Pos/App และ stability run 5 รอบผ่าน ≥95% |
| AUT-DOC-004 | กำหนด Reliability KPI | TODO | - | flaky rate <5%, maintenance effort <20%, queue/runtime SLA และ pass-rate target |
| AUT-DOC-005 | Operational Runbook | TODO | - | ครอบคลุมติดตั้ง Agent, rotate secret, recovery, retry, data restore และ incident |

## 10. ลำดับดำเนินงานที่แนะนำ

1. `AUT-P0-003` ถึง `AUT-P0-006` — Object Repository และ Verification
2. `AUT-P0-007` ถึง `AUT-P0-011` — Maintenance/Failure/Retry/Quarantine
3. `AUT-TEST-001` ถึง `AUT-TEST-010` — Automated Tests
4. `AUT-P1-001` ถึง `AUT-P1-004` — Persistent Automation Suite
5. `AUT-P1-005` ถึง `AUT-P1-009` — Schedule และ Build Trigger
6. `AUT-DATA-001` ถึง `AUT-DATA-006` — Test Data Management
7. `AUT-REG-001` ถึง `AUT-REG-006` — Regression Closed Loop
8. `AUT-P2-001` ถึง `AUT-P2-008` — Monitoring, UX และ Scalability

## 11. Progress Log

### 2026-08-27 — AUT-P1-009: Schedule Notifications (ปิดครบทั้ง section 5.2 ข้อ Schedule/Trigger 5 ใน 5 — จบทั้งกลุ่ม AUT-P1-005 ถึง 009)

- ปิด `AUT-P1-009` — งานสุดท้ายในกลุ่ม `AUT-P1-005` ถึง `AUT-P1-009` (Schedule/Build Trigger) ตามที่ระบุไว้ใน section 10 ข้อ 5
- **ไม่มี event bus/hook framework ใหม่ — ต่อโค้ดเข้าไปตรงๆ ใน 2 จุดที่มีอยู่แล้ว:** `FireDueSchedulesAsync` (P1-006) หลัง `RunSuiteAsync` สำเร็จ สร้าง Started notification ต่อ 1 Execution ที่สร้างจริง + เช็ค `ListAgentsAsync` สร้าง NoAgent เพิ่มถ้าไม่มี agent enabled เลย และ `CompleteExecutionAsync` (P0 เดิม) หลัง `execution.Complete(...)` สร้าง Completed/Failed ตามผล — ทั้งสองจุดเป็นโค้ดที่มีอยู่แล้วและถูกทดสอบมาอย่างดีตั้งแต่ P0/P1-006 การเพิ่ม notification เข้าไปจึงไม่กระทบ business logic เดิมเลย (แค่เขียน record เพิ่มหลัง state เปลี่ยนแล้ว)
- **ปัญหาที่ต้องแก้ก่อนเขียนโค้ด: จะรู้ได้ยังไงตอน Complete ว่า Execution นี้มาจาก schedule fire?** ไม่อยากเพิ่ม FK ใหม่บน `AutomationExecution` (entity ที่ RunSuiteAsync/BatchRunAsync/BuildTrigger ใช้ร่วมกันหมด แก้ constructor path จะกระทบวงกว้าง) เลยใช้ notification "Started" ที่สร้างไว้แล้วเป็น mapping ตัวมันเอง — `FindStartedNotificationByExecutionAsync` เช็คว่ามี Started row สำหรับ ExecutionId นี้ไหม ถ้ามีก็รู้ทั้ง ScheduleId และ ProjectId ไปสร้าง Completed/Failed ต่อได้เลยไม่ต้องมี schema ใหม่ — ผลข้างเคียงที่ตั้งใจ: auto-retry execution (สร้างจาก `CompleteExecutionAsync` เองตอน retry) ไม่มี Started ของตัวเอง จึงไม่มี Completed/Failed แยกตอน retry เสร็จ เป็น scope ที่ตัดใจไม่ทำเพื่อไม่ต้อง thread schedule-origin ผ่าน auto-retry constructor
- **ตัดสินใจสำคัญเรื่อง "No Agent" หมายถึงอะไร:** อ่านโค้ด `ClaimNextJobAsync` จริงก่อนออกแบบ พบว่าไม่ได้กรอง job ตาม capability เลย (กรองแค่ TargetApp และเฉพาะไม่ใช่ WindowsUI default — ตรงกับที่ AUT-TEST-006 บันทึกไว้ว่า "capabilities param ถูกรับแต่ไม่ใช้กรองเลย") จึงตัดสินใจไม่เช็ค capability-matching สำหรับ NoAgent (จะให้ความแม่นยำเกินที่ระบบจริงมี ทำให้ error message โกหกผู้ใช้) — เช็คแค่ "มี agent enabled อยู่จริงในระบบไหม" แทน ง่ายกว่าและตรงกับพฤติกรรมจริงของ job claim มากกว่า
- Entity ใหม่ `AutomationScheduleNotification` — `AutomationExecutionId` บังคับเสมอ (ไม่ nullable) เพื่อให้ทุกแถวมีลิงก์ Execution ตรง AC "พร้อมลิงก์ Execution" ทุก event type รวมถึง NoAgent (ผูกกับ Execution แรกที่ schedule สร้างในรอบนั้น เพราะ NoAgent เกิดขึ้นได้ก็ต่อเมื่อมี Execution ถูกสร้างแล้วเท่านั้น)
- Migration `AddAutomationScheduleNotifications` (ตาราง `AutomationScheduleNotifications`, FK ไป `AutomationSuiteSchedules` Cascade) — apply สำเร็จบน dev DB จริง
- Endpoint ใหม่ 4 ตัวใน `AutomationSchedulesController` ใช้ policy `AutomationView` เดิม (mark-read ไม่ใช่การแก้ automation config จึงไม่ต้อง `AutomationEdit`)
- Frontend: ปุ่ม "🔔 การแจ้งเตือน" + badge unread count ใน header ของ Schedule tab เดิม (ไม่แยก tab ใหม่ เพราะเป็น feature ผูกกับ Schedule โดยตรงตามชื่อ AC) เปิด modal แสดงรายการพร้อมปุ่ม "ดู Execution" ที่ดึงจาก `GET executions/{id}` เดิมแล้วเปิด Execution Detail modal เดิมของหน้า Execution ตรง — ไม่สร้าง Execution viewer ใหม่ซ้ำ
- Test: `AutomationScheduleNotificationTests.cs` ใหม่ 9 tests (รายละเอียดเต็มในตารางแถว AUT-P1-009 ด้านบน)
- Build: QA Hub `dotnet build` 0 warning/0 error, `dotnet test` **257/257 ผ่าน** (เพิ่มจาก 248); frontend `npm run build` (tsc+vite)/`npm run lint` (oxlint) ผ่านทั้งคู่, `git diff --check` ผ่าน

### 2026-08-27 — AUT-P1-008: CI/CD Webhook Integration (ปิดครบทั้ง section 5.2 ข้อ Schedule/Trigger 4 ใน 5)

- ปิด `AUT-P1-008` — ทำต่อทันทีหลัง P1-007
- **Auth model แยกจากระบบหลักทั้งหมด:** endpoint webhook จริง (`POST /api/v1/webhooks/automation/builds`) ไม่มี `[Authorize]` เลยที่ระดับ ASP.NET Core (route อยู่นอก `api/v1/automation/*` โดยตั้งใจ ให้เห็นชัดว่าเป็นคนละ trust boundary) — auth เองด้วยมือใน service โดย hash token ที่ส่งมาผ่าน header `X-Webhook-Token` แล้วค้นหาใน DB; ตรวจสอบก่อนว่า `RequireProjectAccess` filter เดิม (`ProjectAccessFilter.cs`) จะ no-op เงียบๆ ถ้าไม่มี JWT user (ไม่ throw, ไม่ block) เลยตัดสินใจไม่ใช้ attribute นี้เลยกับ controller นี้เพื่อไม่ให้เข้าใจผิดว่ามันช่วย gate อะไรได้จริง
- **Token ผูก project ในตัวเอง:** request body ไม่มี field `projectId` เลย — resolve จาก token ที่ authenticate ผ่านโดยตรง ป้องกันเคส token โปรเจกต์ A ถูกเอาไปยิงใส่ releaseId ของโปรเจกต์ B (ถ้าให้ client ส่ง projectId มาแยกแล้วแค่เช็คว่า token.ProjectId==body.projectId จะยังพอป้องกันได้เหมือนกัน แต่การไม่มี field นี้เลยตัดความเป็นไปได้ที่จะ implement ผิดพลาดออกไปทั้งหมด)
- **บั๊กที่จับได้ระหว่างออกแบบ (ก่อนเขียนโค้ดจริงด้วยซ้ำ):** ตั้งใจแรกจะใส่ DB unique index บน (ProjectId, RequestId) ตรงๆ เพื่อบังคับ anti-replay ที่ระดับ DB — แต่คิดต่อแล้วพบว่าจะทำให้ retry-after-failure พังไปด้วย (เช่น ครั้งแรก releaseId พิมพ์ผิดใน CI config, แก้แล้ว retry ด้วย RequestId เดิมจะชนกับ record เดิมที่เป็น "Failed") จึงเปลี่ยนดีไซน์เป็น non-unique index + application-level lookup ที่กรองเฉพาะ delivery ที่เคยสำเร็จ (`Status="Created"`) เท่านั้นถึงจะนับเป็น "ซ้ำ" — เทสต์ `A_failed_delivery_does_not_block_a_retry_with_the_same_RequestId` ยืนยันพฤติกรรมนี้โดยเฉพาะ
- **Token security:** ใช้ `RandomNumberGenerator.GetBytes(32)` (256-bit) ไม่ใช่ GUID (122-bit) เพื่อ entropy สูงกว่า; hash ด้วย SHA-256 ธรรมดา ไม่ใช่ bcrypt/PBKDF2 แบบ password เพราะ secret นี้สุ่มเต็ม 256-bit อยู่แล้วไม่มีความเสี่ยง brute-force แบบ human-chosen password ที่ slow-hash ออกแบบมาป้องกัน (และ webhook ถูกเรียกบ่อยกว่า login มาก การ hash ช้าโดยไม่จำเป็นจะเสียเวลาทุก request)
- **Reuse เต็มรูปแบบกับ P1-007:** `ReceiveBuildAsync` เรียก `ReleaseService.CreateBuildAsync` ตัวเดียวกับ manual create — Build ที่เกิดจาก webhook จึงยิง Smoke trigger policy อัตโนมัติโดยไม่ต้องเขียนอะไรเพิ่มเลย เป็นตัวอย่างที่ 3 ติดต่อกัน (หลัง P1-004 RunSuiteAsync, P1-006 FireDueSchedulesAsync) ที่ฟีเจอร์ใหม่ต่อยอดจาก primitive เดิมแทนสร้าง path คู่ขนาน
- Token management (CRUD ปกติ, auth ด้วย JWT) แยกคนละ controller (`AutomationWebhookTokensController`) จาก endpoint รับ webhook จริง (`AutomationBuildWebhookController`) เพื่อไม่ให้ auth model 2 แบบมาปนกันในไฟล์เดียว
- Frontend: tab "Webhook" ใหม่ — สร้าง/เพิกถอน token, modal แสดง plaintext ครั้งเดียวพร้อมคำเตือนชัดเจนว่าดูซ้ำไม่ได้, ตารางประวัติการเรียก webhook
- Test: `AutomationWebhookTests.cs` ใหม่ 11 tests (รายละเอียดเต็มในตารางแถว AUT-P1-008 ด้านบน)
- Build: QA Hub `dotnet build` 0 warning/0 error, `dotnet test` **248/248 ผ่าน** (เพิ่มจาก 237); frontend `npm run build` (tsc+vite)/`npm run lint` (oxlint) ผ่านทั้งคู่, `git diff --check` ผ่าน
- **ขอบเขตที่ตั้งใจไม่ทำตอนนี้:** รับเฉพาะ `releaseId` (GUID) ไม่รองรับ `releaseCode` (string ที่ CI config จำง่ายกว่า) — เป็น UX improvement ที่ทำทีหลังได้ไม่กระทบ AC หลัก, ไม่มี rate-limiting บน endpoint webhook สาธารณะ (เสี่ยง DoS เชิงทฤษฎีถ้า token รั่ว แต่ token ที่รั่วก็ revoke ได้ทันทีอยู่แล้ว), ยังไม่มี notification เมื่อ webhook delivery fail (รวมกับ `AUT-P1-009` ที่ยังไม่ทำเหมือนเดิม)

### 2026-08-27 — AUT-P1-007: Build Trigger (ปิดครบทั้ง section 5.2 ข้อ Schedule/Trigger 3 ใน 5)

- ปิด `AUT-P1-007` — ทำต่อทันทีหลัง P1-006 ในช่วงเวลาเดียวกัน
- **ปัญหาที่ต้องตัดสินใจก่อนเริ่ม:** AC เดิมพูดถึง "Build ใหม่ trigger Smoke/Regression ตาม policy" แต่ `CreateBuildRequest`/`CreateBuildAsync` เดิมไม่มีช่องทางรู้ว่า build เป็น RC หรือไม่ตอนสร้าง (สถานะ `IsReleaseCandidate` ถูกตั้งแยกทีหลังผ่าน `MarkRcAsync` เท่านั้น) — ถ้าไม่ตัดสินใจอะไรเพิ่ม การแยก Smoke/Regression จะเป็นแค่ label ที่ไม่มีผลจริงต่อพฤติกรรม จึงเลือกผูกความหมายจริงเข้ากับ 2 event คนละจุด: **Smoke → ทุกครั้งที่สร้าง Build ใหม่**, **Regression → เฉพาะตอน mark RC** — ตรงกับ pattern การทำงานจริงทั่วไป (smoke เร็วรันบ่อย, regression หนักรันก่อน release) และทำให้ field `Pack` มีความหมายเชิงพฤติกรรมจริง ไม่ใช่แค่ label
- **สถาปัตยกรรม cross-namespace:** `ReleaseService` (namespace `Releases`) ต้อง inject `AutomationBuildTriggerService` (namespace `Automation`) ตรงๆ — ตรวจสอบก่อนว่าไม่มี dependency ย้อนกลับ (Automation → Releases) และพบ precedent ที่มีอยู่แล้วในโค้ดเดิม (`TestSummaryService(ReleaseService,DashboardService)`, `AutomationCaseService` พึ่ง `ITestCaseRepository` ข้าม namespace) — ทั้งคู่อยู่ assembly `ProMaxx2.QA.Application` เดียวกันจึงไม่มีปัญหา circular project reference ระดับ .csproj แม้จะมี circular *namespace* dependency ในทางทฤษฎีถ้าออกแบบผิดทาง (ตรวจแล้วว่าไม่เกิดขึ้นจริงเพราะทิศทางเดียว)
- **Best-effort เสมอ:** `FireForBuildAsync` catch exception กว้าง (`when (ex is not OperationCanceledException)`) แบบเดียวกับ `FireDueSchedulesAsync` ใน AUT-P1-006 — เหตุผลสำคัญกว่าที่นี่ด้วยซ้ำ เพราะ trigger นี้ฝังอยู่ตรงกลาง flow การสร้าง Build/Mark RC ที่ผู้ใช้กำลังรอผลอยู่ ถ้า policy พังแล้วโยน exception ขึ้นไปจะทำให้ action หลักที่ผู้ใช้ตั้งใจทำ (สร้าง build) ล้มเหลวไปด้วยทั้งที่ไม่เกี่ยวข้องกันเลย — ทดสอบยืนยันด้วย `A_closed_suite_records_a_failed_run_but_does_not_fail_build_creation`
- reuse `AutomationAgentService.RunSuiteAsync`/`IAutomationSuiteRepository` เดิมทั้งหมด ไม่มี execution logic ใหม่เกิดขึ้นเลยในงานนี้ — เหมือนกับที่ AUT-P1-004/006 ทำมาตลอด (ตัวอย่างที่ดีต่อเนื่องของการไม่สร้างระบบรันคู่ขนาน)
- Migration `AddAutomationBuildTriggers`: ตาราง `AutomationBuildTriggerPolicies` + `AutomationBuildTriggerRuns` — apply สำเร็จบน dev DB จริง ไม่มีปัญหาชื่อชนกับตารางเก่าเหมือนตอน AUT-P1-005 (ชื่อใหม่ทั้งคู่ไม่เคยมีมาก่อน)
- Frontend: tab "Build Trigger" ใหม่ (โครงเดียวกับ Schedule tab — list/create-edit modal/ประวัติการรัน modal) — ไม่มี Build select ในฟอร์ม policy เพราะ policy ไม่ได้ผูกกับ build เจาะจง (BuildId มาจาก event ตอนที่ trigger จริง ไม่ใช่ config)
- Test: `AutomationBuildTriggerTests.cs` ใหม่ 9 tests — ตัดสินใจทดสอบผ่าน `ReleaseService` จริง (ไม่เรียก `FireForBuildAsync` ตรงๆ) เพื่อให้ครอบคลุม wiring จริงใน `CreateBuildAsync`/`MarkRcAsync` ไม่ใช่แค่ logic ของ trigger service เดี่ยวๆ
- Build: QA Hub `dotnet build` 0 warning/0 error, `dotnet test` **237/237 ผ่าน** (เพิ่มจาก 228); frontend `npm run build` (tsc+vite)/`npm run lint` (oxlint) ผ่านทั้งคู่, `git diff --check` ผ่าน
- **ขอบเขตที่ตั้งใจไม่ทำตอนนี้:** ไม่มี delay/debounce ถ้ามีการสร้าง build หลายตัวติดกันเร็วๆ (แต่ละ build เป็น 1 event อิสระ trigger ทันที ไม่รวมกลุ่ม — ตรงตาม AC ที่ไม่ได้ขอ debounce), ยังไม่มี notification เมื่อ trigger fail (นั่นคือ `AUT-P1-009` ที่แยกไว้ต่างหากเหมือนเดิม), `AUT-P1-008` (CI/CD Webhook) ยังไม่ทำ — เป็นอีกช่องทางกระตุ้น Build Trigger จากภายนอกที่ยังไม่ implement

### 2026-08-27 — AUT-P1-006: Schedule Execution Worker (background service ตัวแรกในระบบ)

- ปิด `AUT-P1-006` — ทำต่อจาก `AUT-P1-005` ทันทีในช่วงเวลาเดียวกัน (ยังไม่ commit AUT-P1-005 ก็ทำต่อได้เพราะ migration ก่อนหน้า apply ขึ้น dev DB แล้ว ของใหม่ต่อยอดด้วย migration ใหม่)
- **สถาปัตยกรรม:** แยก timer loop (`AutomationScheduleWorker : BackgroundService`, poll 30s) ออกจาก business logic ทั้งหมด (`AutomationAgentService.FireDueSchedulesAsync`) — ทำให้ core logic ทดสอบได้แบบ deterministic โดยไม่ต้องรอ timer จริงหรือ mock เวลา เพียงเรียก `FireDueSchedulesAsync(nowUtc, ct)` ด้วย `nowUtc` ที่กำหนดเอง
- **Exactly-once claim ("สร้าง Job ครั้งเดียวตามเวลา"):** `ClaimDueSchedulesAsync` ใช้ Serializable transaction เหมือน `ClaimNextJobAsync`/`ClaimVerificationBatchAsync` ที่มีอยู่แล้ว — SELECT schedule ที่ due แล้วเรียก `RecordFired()` (advance/deactivate) **ก่อน** commit ในธุรกรรมเดียวกัน ไม่ใช่หลังรันจริงเสร็จ ดังนั้นแม้ 2 tick จะทับซ้อนกันพอดี (หรือถ้าในอนาคต scale เป็นหลาย instance) tick ที่สองจะไม่เห็น schedule นั้นเป็น due อีกแล้วเพราะ NextRunAtUtc ถูกขยับไปแล้วในธุรกรรมแรก — พิสูจน์ด้วยเทสต์ที่เรียก `FireDueSchedulesAsync` สอง tick ติดกันด้วย `nowUtc` เดียวกันเป๊ะแล้วยืนยันสร้าง execution แค่ 1 รายการ
- **Recovery หลัง restart ได้มาฟรี:** ตัดสินใจไม่เขียน logic พิเศษสำหรับ "schedule ที่พลาดเวลาไปตอน app ปิดอยู่" เพราะ design ของ `NextRunAtUtc` (ขยับไปข้างหน้าเฉพาะตอน claim สำเร็จเท่านั้น ไม่มีที่ไหนอื่นที่แก้ค่านี้) ทำให้ schedule ที่ due ไปแล้วแต่ยังไม่ถูก claim จะยังคง `NextRunAtUtc <= now` อยู่เสมอไม่ว่าจะผ่านไปนานแค่ไหน — tick แรกหลัง restart จะเห็นเป็น due ทันทีโดยอัตโนมัติ ยืนยันด้วยเทสต์ที่เรียก fire ด้วย `nowUtc` ที่ห่างจากตอนสร้าง schedule ไป 2 วัน (จำลอง "แอปปิดอยู่ 2 วันพอดีตอนถึงเวลาที่ควรรัน") แล้วยัง fire ได้ถูกต้อง
- **Audit ("audit ผล"):** entity ใหม่ `AutomationScheduleRun` บันทึกทุกครั้งที่ worker พยายามรัน schedule ไม่ว่าผลจะเป็น Succeeded (สร้าง execution ได้)/NoReadyCases (รันสำเร็จแต่ไม่มี case พร้อมรัน)/Failed (suite ปิด/ถูกลบ/ไม่มี case เลย — `RunSuiteAsync` throw) — ตัดสินใจ catch exception กว้าง (`catch (Exception ex) when (ex is not OperationCanceledException)`) แทนจำกัดเฉพาะ exception type ที่รู้จัก เพราะ schedule หนึ่งตัวพังต้องไม่ทำให้ schedule อื่นในรอบเดียวกันไม่ถูกประมวลผลและต้องมี audit record เสมอ ไม่ใช่หายเงียบ
- API: `GET /api/v1/automation/schedules/{id}/runs` (policy `AutomationView`); frontend เพิ่มคอลัมน์ "รันล่าสุด" และปุ่ม "ประวัติการรัน" เปิด modal
- Test: `AutomationScheduleWorkerTests.cs` ใหม่ 8 tests (รายละเอียดเต็มในตารางแถว AUT-P1-006 ด้านบน)
- Build: QA Hub `dotnet build` 0 warning/0 error, `dotnet test` **228/228 ผ่าน** (เพิ่มจาก 220); frontend `npm run build` (tsc+vite)/`npm run lint` (oxlint) ผ่านทั้งคู่, `git diff --check` ผ่าน
- **ขอบเขตที่ตั้งใจไม่ทำตอนนี้:** poll interval hardcode 30s ไม่มี config (ยังไม่มี convention ให้ตามในระบบตัวแรกนี้), ไม่มีเทสต์ตรง `AutomationScheduleWorker`/`PeriodicTimer` เอง (เป็นแค่ wrapper บางๆ รอบ `FireDueSchedulesAsync` ที่ทดสอบครบแล้ว), ยังไม่มี notification เมื่อ schedule รันแล้ว fail (นั่นคือ `AUT-P1-009` Schedule Notifications ที่แยกไว้ต่างหาก)

### 2026-08-27 — AUT-P1-005: Persistent Automation Schedule (เริ่ม section 5.2 "Schedule และ Build Trigger")

- ปิด `AUT-P1-005` — งานแรกในกลุ่ม `AUT-P1-005` ถึง `AUT-P1-009` (Schedule/Build Trigger); ยังไม่แตะ `AUT-P1-006` (worker ที่ยิงรันจริงตามเวลา) — งานนี้เป็นแค่ persistent config + คำนวณ next-run
- Domain: `AutomationSchedule` ใหม่ — Frequency (`Once`/`Daily`/`Weekly`) + `DaysOfWeekMask` (bitmask `1 << (int)DayOfWeek`, ใช้เฉพาะ Weekly) + `RunAtTime`/`OnceOnDate` + `TimeZoneId` + target Build/Environment/Agent/Priority (โครงเดียวกับ `RunSuiteRequest`) + `IsActive`/`NextRunAtUtc`; `ComputeNextRunUtc` แปลง wall-clock ใน timezone ที่เลือกเป็น UTC ผ่าน `TimeZoneInfo` (ถูกต้องข้าม DST เพราะคำนวณเป็น local time ก่อนแล้วค่อยแปลงกลับ ไม่ใช่บวก/ลบ offset คงที่); Weekly สแกนไปข้างหน้า 8 วันหาแรกที่ตรงกับวันที่เลือกและยังไม่ผ่านไป, Once ต้องเป็นอนาคตไม่งั้น throw
- Migration `AddAutomationSchedules`: ตั้งชื่อตาราง SQL เป็น `AutomationSuiteSchedules` (ไม่ใช่ `AutomationSchedules`) เพราะตรวจพบว่าชื่อ `AutomationSchedules` มีตาราง orphan อยู่แล้วบน dev DB จริงจาก Automation module เวอร์ชันเก่า (migration `AddAutomationModuleV2` สร้างไว้ตอนรีบิลด์ระบบรอบก่อน ผูกกับโมเดล Release/Build แบบเก่า ไม่มี C# entity ใดแม็พอยู่ในปัจจุบัน) — ถ้าใช้ชื่อเดิมจะ apply ไม่ได้เพราะ SQL Server มี object ชื่อนั้นอยู่แล้ว; เลือกเปลี่ยนชื่อตาราง SQL แทนการลบตารางเก่า (ไม่แน่ใจ 100% ว่าไม่มีอะไรอ้างอิงอยู่นอกโค้ดที่เห็น) — apply migration ใหม่สำเร็จบน dev DB จริง ไม่ชนกับตารางเก่า
- Service: `AutomationScheduleService` (Create/Update/Activate/Deactivate/List/Get) ตรวจ Suite ต้องมีอยู่จริงใน project ผ่าน `IAutomationSuiteRepository.FindSuiteAsync` ก่อนสร้าง schedule (reuse interface เดิมจาก AUT-P1-001 แทนสร้างซ้ำ); `AutomationRepository` เพิ่ม `IAutomationScheduleRepository` เป็น interface ที่ 3 (ตามแพทเทิร์นเดิมของ `IAutomationSuiteRepository`)
- API: `AutomationSchedulesController` (`GET/POST api/v1/automation/schedules`, `GET/PUT {id}`, `POST {id}/activate`, `POST {id}/deactivate`) ใช้ policy `AutomationView`/`AutomationEdit` เดิมทั้งหมด (ไม่มี endpoint สั่งรันตรงๆ ในงานนี้ — ผู้ใช้ยังต้องรอ worker หรือกด "▶ รัน" ที่ suite เองถ้าต้องการรันทันที)
- Frontend: เพิ่ม tab "Schedule" ใหม่ในหน้า Automation — ตาราง list พร้อมข้อความอธิบายตารางเวลาแบบอ่านง่าย (`describeSchedule`), filter active/inactive, create/edit modal (`ScheduleFormModal`) มีตัวเลือก Suite (ล็อกไม่ให้เปลี่ยนตอนแก้ไข — schedule ผูกกับ suite เดียวตลอดอายุ), Frequency พร้อม UI ที่ปรับเปลี่ยนตามค่า (Weekly โชว์ checkbox 7 วัน, Once โชว์ date picker), Timezone select ดึงจาก `Intl.supportedValuesOf("timeZone")` (fallback เป็น list คงที่ถ้า browser เก่าไม่รองรับ), Build/Environment/Agent/Priority เหมือน `RunSuiteModal`; ปุ่มเปิด/ปิดใช้งานพร้อม confirm
- Test: `AutomationScheduleTests.cs` ใหม่ 13 tests — Daily/Weekly/Once คำนวณ next-run ถูกต้อง (รวม assert ตรง time-of-day และช่วงวันที่สมเหตุสมผล), Weekly ไม่เลือกวันเลย throw, Once ในอดีต throw, timezone id ผิด throw, แปลงเวลาข้าม timezone จริงด้วย "SE Asia Standard Time" (09:00 Bangkok = 02:00 UTC พิสูจน์ conversion ทำงานจริงไม่ใช่แค่ echo กลับ), suite ไม่มีอยู่จริง throw `EntityNotFoundException`, Update เปลี่ยนเวลาแล้ว next-run ขยับตาม, Activate/Deactivate สลับสถานะได้และ guard เรียกซ้ำ (throw `InvalidOperationException`), List filter ตาม isActive
- Build: QA Hub `dotnet build` 0 warning/0 error, `dotnet test` **220/220 ผ่าน** (เพิ่มจาก 207); frontend `npm run build` (tsc+vite)/`npm run lint` (oxlint) ผ่านทั้งคู่, `git diff --check` ผ่าน

### 2026-08-27 — AUT-P1-004: Run Suite ซ้ำ (ปิดครบทั้ง P1-001 ถึง P1-004)

- ปิด `AUT-P1-004` — ปิดครบทุก item ใน "P1 — Automation Suite" (section 5 ข้อ 1-4)
- Service: `AutomationAgentService.RunSuiteAsync` — inject `IAutomationSuiteRepository` เพิ่มเข้า `AutomationAgentService` (เดิมมีแค่ `IAutomationRepository`) เพื่อดึง case membership ของ suite แล้วส่งต่อให้ `BatchRunAsync` เดิมทำงานทั้งหมด (Ready/Quarantine filter ที่แก้ไปตอน code review ก่อน merge PR #1 จึงมีผลกับ Run Suite ด้วยโดยอัตโนมัติ ไม่ต้องเขียนซ้ำ) — ตัวอย่างที่ดีของการ reuse แทนสร้าง logic คู่ขนานใหม่ (ตรงข้ามกับ finding AUT-P0-015 ที่เตือนเรื่อง Suite ซ้ำ pattern กับ TestSuite)
- API: `POST /api/v1/automation/suites/{id}/run` policy `AutomationExecute` (ไม่ใช่ `AutomationEdit` เหมือน endpoint suite อื่น เพราะเป็น action สั่งรัน ไม่ใช่แก้ config) รับ `RunSuiteBodyRequest` (Build/Environment/Agent/Priority — ไม่มี suiteId ซ้ำในตัว body)
- Frontend: ปุ่ม "▶ รัน" ทั้งจาก row action ในตาราง list และปุ่มใน suite detail modal action bar (เฉพาะเมื่อ `canRun` และ suite active) เปิด `RunSuiteModal` เลือก Build/Environment/Priority (โครงเดียวกับ `BatchRunModal` เดิมแต่ไม่มีตัวเลือก case) แสดง modal สรุปผล created/skipped หลังสั่งรัน
- Test: `AutomationSuiteRunTests.cs` ใหม่ 6 tests
- Build: QA Hub `dotnet build` 0 warning/0 error, `dotnet test` **207/207 ผ่าน** (เพิ่มจาก 201); frontend `npm run build`/`npm run lint` ผ่านทั้งคู่, `git diff --check` ผ่าน

### 2026-08-27 — AUT-P1-003: Suite Version/History (branch ใหม่ `feat/automation-suite-versioning` หลัง merge PR #1)

- ปิด `AUT-P1-003` — เริ่มงานแรกบน branch ใหม่ที่แตกจาก `main` หลัง merge PR #1
- Domain: `AutomationSuiteRevision` (audit-trail entry ต่อการเปลี่ยนแปลง 1 ครั้ง) + `AutomationSuite.RevisionNo`/`BumpRevision()`; constructor set `RevisionNo=1` ตอนสร้าง (revision แรก = "Created")
- Migration `AddAutomationSuiteRevisions`: เพิ่มคอลัมน์ `RevisionNo` บน `AutomationSuites` (default 0) แล้ว backfill ด้วย raw SQL `UPDATE ... SET RevisionNo = 1 WHERE RevisionNo = 0` ให้แถวเดิมที่มีอยู่ก่อน migration อ่านค่าถูกต้องเป็น revision 1 เหมือนของใหม่ทุกอัน, สร้างตาราง `AutomationSuiteRevisions` — apply สำเร็จบน dev DB จริง
- Service: ทุก mutation (`UpdateAsync`/`CloseAsync`/`ReopenAsync`/`AddCasesAsync`/`UpdateCaseAsync`/`RemoveCaseAsync`) bump revision แล้วบันทึก `AutomationSuiteRevision` พร้อม Detail ที่สร้างอัตโนมัติให้อ่านง่าย (เช่น rename แสดง "Renamed X → Y", add cases แสดงรายการ AutomationCode ที่เพิ่ม) และรับ `ChangeReason` ไม่บังคับจาก caller ผ่าน request ใหม่ (`UpdateAutomationSuiteRequest`/`SuiteLifecycleRequest`/`AddSuiteCasesRequest`/`UpdateSuiteCaseRequest` เพิ่ม field `ChangeReason`)
- แก้ไขให้ idempotent add (กรณี case ที่ขอเพิ่มมีอยู่ในซุยท์ทั้งหมดแล้ว ไม่มีอะไรถูกเพิ่มจริง) ไม่สร้าง revision entry ปลอม (เช็ค `addedIds.Count > 0` ก่อนบันทึก)
- API: `GET /api/v1/automation/suites/{id}/history` คืนรายการ revision เรียงล่าสุดก่อน พร้อม resolve ชื่อผู้แก้จาก Users table
- Frontend: ปุ่ม "ประวัติ" ทั้งจาก row ในตาราง list และในปุ่ม action bar ของ suite detail modal เปิด modal ประวัติแยกต่างหาก แสดง RevisionNo/ChangeType/Detail/เหตุผล/ผู้แก้/เวลา; เพิ่มช่อง "เหตุผลที่แก้ไข" (ไม่บังคับ) ในหน้าฟอร์มแก้ไข suite และ modal เพิ่ม case; แสดง RevisionNo ปัจจุบันในหัว suite detail modal; ตัดสินใจไม่เพิ่มช่องเหตุผลให้ปุ่ม close/reopen/remove-case แบบ one-click confirm (คงไว้เป็น `window.confirm` เหมือนเดิมทั่วทั้งแอป) เพื่อไม่ทำลาย UX ที่มีอยู่ — เหตุผลยัง optional ผ่าน backend ได้ถ้าต้องการเพิ่มภายหลัง
- Test: `AutomationSuiteHistoryTests.cs` ใหม่ 7 tests
- Build: QA Hub `dotnet build` 0 warning/0 error, `dotnet test` **201/201 ผ่าน** (เพิ่มจาก 194); frontend `npm run build` (tsc+vite)/`npm run lint` (oxlint) ผ่านทั้งคู่, `git diff --check` ผ่าน

### 2026-08-27 — Code review ก่อน merge PR #1 (AUT-P0-014/015/016)

- ผู้ใช้ขอให้ review ก่อน merge PR #1 (`feat/automation-reliability-tests` → `main`, 8 commits) — รัน `/code-review` ระดับ high, 7 finder agent (angle A/B/C, Reuse, Simplification, Efficiency, Altitude) วิเคราะห์ diff ทั้งหมดแบบขนาน
- **แก้ 7 bug จริง** (รายละเอียดเต็มใน AUT-P0-014): quarantine bypass (Run/Batch Run/auto-retry), maintenance classification whitelist ไม่ตรงกับ classifier, job idempotency guard throw ไม่ถูกจับตอน race, verification ไม่มี idempotency guard, verification batch claim ไม่มี transaction (double-claim race), DB connect timeout lower-bound เป็น 0 ได้ (regression ใน AUT-P0-013 เอง), Suite query ไม่กรอง IsDeleted
- **1 finding เป็น false positive ที่ verify แล้วตัดออก:** finder ตัวหนึ่งรายงาน `RunTryLoginAsync`/"trylogin" credential tool ว่าเป็นโค้ดใหม่ใน PR นี้ — ตรวจสอบด้วย `git diff main...feat/automation-reliability-tests` แล้วพบว่าเป็นโค้ดเดิมที่มีอยู่ใน `main` ก่อนแล้ว ไม่ใช่ส่วนหนึ่งของ PR นี้ จึงไม่นับเป็น finding ของรอบนี้
- **เปิด backlog ใหม่จาก finding ที่ตั้งใจไม่แก้ตอนนี้:** AUT-P0-015 (Automation Suite ซ้ำ pattern กับ TestSuite ทั้ง backend/frontend — refactor ใหญ่), AUT-P0-016 (RetrySafety "Conditional" ความหมายไม่ชัด ต้องตัดสินใจ product ก่อน)
- **บันทึกไว้เฉยๆ ไม่เปิด backlog แยก (severity ต่ำ/เป็น pre-existing pattern เดิม/performance ที่ยังไม่กระทบ scale ปัจจุบัน):** N+1 queries หลายจุด (`GetFlakyCandidatesAsync`, `RequestObjectVerificationAsync`, `GetDashboardAsync` 9 query แยก, `BatchRunAsync` 6 round-trip/case, `AutomationSuiteRepository.ListSuitesAsync` correlated subquery, `RunVerifyAsync` sequential HTTP report ทีละ item), `CompleteExecutionAsync` ทำหลายหน้าที่ในเมธอดเดียว (retry pipeline ควรแยกเป็น abstraction ต่างหากในอนาคต), `Problem()`/`UserId()` helper ถูก copy-paste ซ้ำใน controller ใหม่ (pattern เดิมที่มีอยู่แล้วในหลาย controller), generic `POST cases/{id}/status` endpoint เดิมยังตั้ง MaintenanceRequired ตรงๆ ได้โดยไม่ผ่าน `RequireMaintenance` (ไม่ populate reason/openedAt), `IAutomationSuiteRepository` DI ใช้ downcast จาก `IAutomationRepository` (เปราะถ้ามี decorator ในอนาคต)
- Test: เพิ่ม `AutomationCodeReviewFixTests.cs` ใหม่ (QA Hub, 6 tests: quarantine bypass ทั้ง Run/BatchRun/auto-retry, DSL error → MaintenanceRequired, Timeout+UI code → MaintenanceRequired, completion race ไม่ throw), เพิ่ม 1 test ใน `DatabaseValidatorTests.cs` (agent, ConnectTimeout ไม่เป็น 0/ติดลบ)
- Build: QA Hub `dotnet build` 0 warning/0 error, `dotnet test` **194/194 ผ่าน** (เพิ่มจาก 188); agent `dotnet build` 0 warning/0 error, `dotnet test` **50/50 ผ่าน** (เพิ่มจาก 48)
- ใช้ `ReportFindings` รายงานผลรวม 15 findings (7 CONFIRMED+fixed, 8 PLAUSIBLE+skipped พร้อมเหตุผล)

### 2026-08-27 — AUT-P1-002: จัดการ Case ใน Suite (+ frontend UI ให้ทั้ง P1-001/002)

- ปิด `AUT-P1-002`
- Domain: `AutomationSuiteCase` เพิ่มในไฟล์เดียวกับ `AutomationSuite` (ตามแพทเทิร์น TestSuite/TestSuiteCase) — SortOrder/IsRequired, guard sortOrder ≥ 1
- Migration `AddAutomationSuiteCases`: composite PK (AutomationSuiteId, AutomationCaseId), FK ไป AutomationSuites (Cascade — ลบ suite แล้ว membership หายตาม) และ AutomationCases (Restrict — ลบ case ไม่ได้ถ้ายังอยู่ใน suite ไหน) — apply สำเร็จบน dev DB จริง
- Service: `AddCasesAsync` ตรวจทุก case ต้องอยู่ project เดียวกับ suite ก่อน (ArgumentException ถ้าไม่ใช่ — กัน cross-project reference), add case ที่มีอยู่แล้วซ้ำแบบ idempotent (skip ไม่ throw เพื่อ UX ที่เลือกซ้ำได้), sort order auto-increment จาก max เดิม; `UpdateCaseAsync`/`RemoveCaseAsync`/`AddCasesAsync` ทั้งหมดบล็อกด้วย guard เดียวกับที่ suite header ใช้ (ปิด suite แล้วแก้ case ไม่ได้ ต้อง reopen ก่อน) เพื่อความสม่ำเสมอ
- `SuiteCaseDto` ส่ง AutomationType (Target) และ Status ของแต่ละ case กลับมาด้วย — ตรง AC "ตรวจ Target/Status ได้"; `AutomationSuiteListDto` แยก CaseCount/ReadyCaseCount ให้เห็นภาพรวมโดยไม่ต้องเปิด detail
- API: `POST/PUT/DELETE /api/v1/automation/suites/{id}/cases[/{caseId}]`
- **Frontend (ทำให้ทั้ง AUT-P1-001 และ AUT-P1-002 พร้อมกัน ตามที่บอกไว้ตอน P1-001):** เพิ่ม tab ใหม่ "Automation Suite" ใน `AutomationPage.tsx` (component `AutomationSuiteTab` แบบ self-contained fetch เหมือน `FailureDashboardTab` — ไม่แตะ Promise.all หลักของหน้า) — list พร้อม search/filter สถานะ, สร้าง/แก้ไข suite (modal), ปิด/เปิด suite (confirm), detail modal แสดง case list พร้อมปุ่มเลื่อนลำดับ ↑↓ (สลับ sortOrder กับตัวข้างเคียง), toggle Required/Optional, ลบ case, modal เพิ่ม case แบบ multi-select (กรองเฉพาะ case ที่ยังไม่อยู่ใน suite); เพิ่ม CSS เล็กน้อยสำหรับปุ่มเลื่อนลำดับ (`.automation-sort-controls`)
- Test: รวม `AutomationSuiteTests.cs` (11, จาก P1-001) + `AutomationSuiteCaseTests.cs` ใหม่ (11): add/idempotent-add/cross-project-reject/empty-list-reject/closed-suite-reject, update sort+required, remove, list CaseCount vs ReadyCaseCount แยกกันถูกต้อง
- Build: QA Hub `dotnet build` 0 warning/0 error, `dotnet test` **188/188 ผ่าน** (เพิ่มจาก 166); frontend `npm run build` (tsc + vite) ผ่าน, `npm run lint` (oxlint) ผ่าน, `git diff --check` ผ่าน (แค่ CRLF warning ปกติ)

### 2026-08-27 — AUT-P1-001: Persistent Automation Suite (backend)

- ปิด `AUT-P1-001` (เฉพาะ backend — ดูหมายเหตุขอบเขต)
- Domain: `AutomationSuite` (`src/ProMaxx2.QA.Domain/Automation/AutomationSuite.cs`) — Create/Update/Close/Reopen, guard แก้ไข suite ที่ปิดแล้วต้อง reopen ก่อน
- Migration `AddAutomationSuites` สร้างตาราง `AutomationSuites` พร้อม unique index (ProjectId, SuiteCode), FK ไป Projects (Restrict) — apply สำเร็จบน dev DB จริงแล้ว
- Application: `AutomationSuiteContracts.cs` ใหม่ (แยกจาก `AutomationContracts.cs`/`AutomationService.cs` เดิมที่ใหญ่มากแล้ว ตามแพทเทิร์นเดียวกับ `TestSuiteContracts.cs`) — DTO, `IAutomationSuiteRepository`, `AutomationSuiteService`
- Infrastructure: `AutomationRepository` เปลี่ยนเป็น `partial class` implement เพิ่ม `IAutomationSuiteRepository`, ย้าย suite persistence ไปไฟล์ใหม่ `AutomationSuiteRepository.cs` (partial) เพื่อไม่ให้ `AutomationRepository.cs` เดิมใหญ่ขึ้นอีก; DI bind `IAutomationSuiteRepository` ให้ resolve เป็น instance เดียวกับ `IAutomationRepository` ใน scope เดียวกัน (share DbContext)
- API: `AutomationSuitesController` ใหม่ — `GET/POST /api/v1/automation/suites`, `GET/PUT {id}`, `POST {id}/close`, `POST {id}/reopen`; ใช้ policy `AutomationView` (read)/`AutomationEdit` (write) เดิม ไม่ต้องเพิ่ม policy ใหม่
- Test: `AutomationSuiteTests.cs` 11 tests (create explicit/auto-gen code, duplicate code throw, update, close+closedAt, close-already-closed throw, edit-closed throw, reopen clears closed metadata, reopen-already-open throw, list filter by active/search, project scoping)
- **ขอบเขตที่เว้นไว้โดยตั้งใจ:** ยังไม่มี frontend UI และยังไม่มี Case membership (AUT-P1-002) — Suite เปล่าที่ใส่ Case ไม่ได้ใช้งานจริงไม่ได้ ตัดสินใจรอทำ UI พร้อมกันตอน AUT-P1-002 เพื่อไม่ต้องสร้าง UI ซ้ำสองรอบ (ครั้งแรกแบบไม่มี case, ครั้งสองแก้ใหม่ให้มี case)
- Build: `dotnet build` QA Hub solution 0 warning/0 error; `dotnet test` 177/177 ผ่าน (เพิ่มจาก 166)

### 2026-08-27 — AUT-P0-012/013: แก้ 2 gap จาก AUT-TEST-009/010

- ปิด `AUT-P0-012` และ `AUT-P0-013` (ทำตามคำแนะนำของผู้ใช้ให้แก้ก่อนไป P1)
- **AUT-P0-012:** `agent/ProMaxx2.Automation.Runner/Program.cs` เก็บ `StepOutcome` ของ step ที่ fail แล้ว forward ErrorCode/ErrorMessage จริงไปที่ execution-level completion แทน hardcode "AUT-UI-003" — ตรวจแล้วว่า raw FailureType field ไม่กระทบ retry logic (server ใช้ ClassifiedFailureType ที่คำนวณเองจาก ErrorCode เท่านั้น) จึงคงค่าเดิมไว้
- **AUT-P0-013:** เพิ่ม `DbProfile.ConnectTimeoutSeconds` (cap 5s จาก ActionTimeout) set บน connection string ทั้ง Firebird/SQL Server; เพิ่ม `ConnectRetryCount=0` ปิด SqlClient internal retry ที่พบว่าทำให้ยังช้าอยู่แม้ตั้ง ConnectTimeout แล้ว; เปลี่ยนทั้งสอง validator เป็น async จริงทั้ง `OpenAsync`/`ExecuteReaderAsync`/`ReadAsync` รับ `ct` ทำให้ cancellation มีผลจริง
- เพิ่มเทสต์ยืนยันการแก้: SQL Server connection-refused ตอนนี้ fail ใน ~5s (จากเดิม ~14s ที่เคยตัดออกจาก suite เพราะช้าเกินไป) — เอากลับเข้ามาใน `DatabaseValidatorTests.cs` ได้แล้ว, เพิ่ม test ยืนยัน ConnectTimeout cap logic ถูกต้อง
- Build: `dotnet build ProMaxx2.Automation.slnx` 0 warning/0 error; `dotnet test` agent **48/48 ผ่าน ใน ~7 วินาที** (เพิ่มจาก 45, รวม SQL Server connection test ที่เพิ่มกลับมาได้)

### 2026-08-27 — AUT-TEST-010: Database Validator tests (สุดท้ายใน P0 Automated Tests — ปิดครบ 001-010)

- ปิด `AUT-TEST-010` — ปิดครบทุก item ใน section 4 (P0 Automated Tests)
- เพิ่ม `DbAssertionComparer` (`agent/ProMaxx2.Automation.Core/DbAssertion.cs`): แยก Compare operator logic และ parameter-name normalization ออกจาก `FirebirdDbValidator` เดิม (private method ที่เข้าถึงไม่ได้จนกว่าจะต่อ DB สำเร็จ จึงเทสต์ไม่ได้เลยถ้าไม่แยกออกมา) เป็น shared static class
- **แก้ inconsistency จริงที่พบระหว่างแยกโค้ด:** `SqlServerDbValidator` เดิมไม่รองรับ operator (`>=`/`<=`/`!=`/`>`/`<`) เลย ใช้ case-insensitive string equals ตรงๆ ต่างจาก Firebird ที่รองรับครบมาตั้งแต่แรก — ตอนนี้ทั้งสอง validator ใช้ `DbAssertionComparer` ร่วมกันแล้ว behaviour ตรงกัน
- เพิ่ม `DatabaseValidatorTests.cs` 45 tests: Compare ทุก operator + numeric/bool/string fallback, parameter normalization, `DbValidatorFactory.Create` คืน type ถูกต้อง, Firebird validator ต่อ connection ที่ถูกปฏิเสธจริง (127.0.0.1 port ปิด — เร็ว ไม่ต้องมี DB จริง) คืน failure result ถูกต้อง
- **ตัดสินใจไม่ใส่ live-connection test สำหรับ SQL Server:** ลองแล้วพบว่า `Microsoft.Data.SqlClient` ใช้เวลา ~14 วินาทีกว่าจะ fail connection ที่ถูกปฏิเสธ (เทียบกับ Firebird ที่ fail เกือบทันที) เพราะ validator ไม่ได้ set `ConnectTimeout` เลย — ตัดออกจาก suite เพื่อไม่ให้ทั้งชุดช้าลงสำหรับ assertion เดียว แล้วเปิดเป็นงานใหม่ `AUT-P0-013` แทน (รวม CancellationToken ที่ไม่เคยถูกใช้จริงในทั้งสอง validator ด้วย)
- Build: `dotnet build ProMaxx2.Automation.slnx` (agent) 0 warning/0 error; `dotnet test` agent **45/45 ผ่าน ใน 2 วินาที** (หลังตัด live SQL Server test ออก); QA Hub solution sanity-check ยังผ่าน 166/166 เหมือนเดิม

### 2026-08-27 — AUT-TEST-009: Agent ActionExecutor tests + error-code bug fix

- ปิด `AUT-TEST-009`
- สร้าง test project ใหม่ `agent/ProMaxx2.Automation.Core.Tests` (net10.0-windows, xunit) เพิ่มเข้า `ProMaxx2.Automation.slnx`; `FakeUiAutomationDriver` (fake `IUiAutomationDriver` ไม่พึ่ง FlaUI/Windows UI Automation จริง — รันได้ทุกเครื่อง ไม่ต้องมี AUT ติดตั้ง)
- เพิ่ม `ActionExecutorTests.cs` 18 tests: UI action สำเร็จ (CLICK/LOGIN), object ไม่เจอ → AUT-UI-001, driver คืน false (timeout แบบ "condition not met") → AUT-UI-003, driver throw TimeoutException → fallback AUT-UI-003, cancellation ก่อน dispatch, unsupported action, assertion actions (EXPECT_TEXT trimmed compare, EXPECT_VISIBLE/EXPECT_NOT_VISIBLE), DB assertion ไม่มี config → AUT-DB-001, evidence capture เฉพาะตอน fail
- **แก้ bug จริงที่พบระหว่างเขียนเทสต์:** `ActionExecutor.ExecuteAsync` catch(Exception) เดิม hardcode ErrorCode="AUT-UI-003" เสมอ ทั้งที่ exception message ฝัง error code เฉพาะไว้แล้ว (เช่น object-not-found ฝัง "(AUT-UI-001)", DB not configured ฝัง "(AUT-DB-001)") — เพิ่ม `ExtractErrorCode` ดึงโค้ดจาก pattern `(AUT-XXX-NNN)` ท้ายข้อความมาใช้แทน โดย fallback เป็น AUT-UI-003 เมื่อไม่มีโค้ดฝังอยู่ (เช่น raw TimeoutException, unsupported action)
- **เปิดงานใหม่ `AUT-P0-012`:** ระหว่างแก้ bug ข้างต้น พบ gap ที่ใหญ่กว่าและยังไม่แก้ — `agent/ProMaxx2.Automation.Runner/Program.cs:285` hardcode execution-level ErrorCode/FailureType เสมอโดยไม่สนใจ step ที่ fail จริง ทำให้ retry/classification บนฝั่ง server แทบไม่ทำงานกับ execution จริงในสนาม (รายละเอียดในตาราง P0) — ไม่แก้ในรอบนี้เพราะกระทบพฤติกรรม retry จริง ต้องให้ทีมตัดสินใจก่อน
- Build: `dotnet build ProMaxx2.Automation.slnx` (agent solution) 0 warning/0 error; `dotnet test` agent 18/18 ผ่าน; sanity-check QA Hub solution ยังผ่าน 166/166 เหมือนเดิม (ไม่ถูกกระทบ เพราะคนละ solution)

### 2026-08-27 — AUT-TEST-008: Automation permission tests

- ปิด `AUT-TEST-008`
- รีแฟกเตอร์ `Program.cs`: ย้าย 8 บรรทัด `.AddPolicy("Automation...")` ออกเป็น `AutomationAuthorizationPolicies.AddAutomationPolicies()` extension method ใหม่ใน `src/ProMaxx2.QA.Api/Services/AutomationAuthorizationPolicies.cs` — เป็น single source of truth ที่ทั้ง Program.cs และเทสต์เรียกใช้ร่วมกัน ป้องกันเทสต์ drift จากของจริง
- เพิ่ม `AutomationAuthorizationPolicyTests.cs` 25 tests: build `IAuthorizationService` จริงบน `ServiceCollection` เปล่าๆ ผ่าน `AddAuthorizationCore` + policy set ข้างต้น (ไม่ต้องมี host) แล้วยิง `AuthorizeAsync` ตรงๆ — ครอบคลุมทั้ง 8 policy (View/Edit/Validate/Approve/Execute/Manage/Evidence/GenerateAI): ผ่านเมื่อมี claim ตรง, fail เมื่อไม่มี claim เลย, fail เมื่อมี Automation permission อื่นที่ไม่ตรง (กัน cross-privilege), AutomationExecute ผ่านได้ 3 ทาง (2 claim + SYS_ADMIN role) ตาม `RequireAssertion` เดิม
- **ขอบเขตที่ไม่ครอบคลุม (ตั้งใจ):** controller-level wiring (endpoint ไหนผูก policy ไหน) และ `[RequireProjectAccess]` filter ยังต้องมี hosted pipeline จริงถึงทดสอบได้ — ตรงกับ note ที่ทิ้งไว้ตอน AUT-TEST-007
- Build: `dotnet build ProMaxx2.QA.slnx` 0 warning/0 error; `dotnet test` ผ่านทั้งชุด 166/166 (เพิ่มจาก 141)

### 2026-08-27 — AUT-TEST-007: Evidence security tests

- ปิด `AUT-TEST-007`
- เพิ่ม `AutomationEvidenceSecurityTests.cs` 8 tests ครอบคลุม type/size validation (ทั้ง step evidence และ execution evidence upload), path-traversal guard ผ่าน `evidenceType` (free text ที่ถูกฝังลง path ตรงๆ โดยไม่ sanitize — ทดสอบว่า guard `full.StartsWith(root)` บล็อกได้จริงก่อนเขียนไฟล์), NotFound + orphan-file cleanup เมื่อ step/execution ไม่มีจริง, upload สำเร็จเขียนเฉพาะโฟลเดอร์ execution ตัวเอง, `GetEvidencePathAsync` ถูก scope ตาม projectId
- เทคนิค: instantiate `AutomationAgentController` ตรงๆ ด้วย fake `IWebHostEnvironment` (ไม่ต้องพึ่ง `WebApplicationFactory`/`Mvc.Testing` ที่ถอดออกไปแล้วตอน AUT-TEST-001) เพราะ method เหล่านี้ไม่แตะ HttpContext
- **ขอบเขตที่ไม่ครอบคลุม (ตั้งใจ):** `[Authorize(Policy="AutomationEvidence")]` และ `[RequireProjectAccess]` filter บน `AutomationController` ต้องมี ASP.NET pipeline จริงถึงทดสอบได้ (WebApplicationFactory) — ยกให้ AUT-TEST-008 (Permission tests) ทำแยกแทนที่จะ fake ผ่านไปในรอบนี้
- Build: `dotnet build ProMaxx2.QA.slnx` 0 warning/0 error; `dotnet test` ผ่านทั้งชุด 141/141 (เพิ่มจาก 133)

### 2026-08-27 — AUT-TEST-006: Batch Run/Multi-Agent tests

- ปิด `AUT-TEST-006`
- เพิ่ม `AutomationTestFixtures.SeedReadyCasesAsync` helper (สร้างหลาย Ready case ใน baseline เดียวกัน สำหรับเทสต์ batch)
- เพิ่ม `AutomationBatchRunTests.cs` 7 tests: batch create/skip, dedup, empty request throw, 2-agent concurrent drain ไม่ชนกัน+ผลรวมครบ, target routing (ทั้งกรณี non-default poller ถูก filter และกรณี default WindowsUI poller ไม่ถูก filter เลย), requested-agent ไม่ถูกบังคับ
- **ข้อสังเกตสำคัญ (พบระหว่างเขียนเทสต์ ต่างจากที่คาดไว้ตอนแรก):** target-based routing ใน `ClaimNextJobAsync` ทำงานแค่ทางเดียว — เงื่อนไข `if (target != "WindowsUI")` แปลว่า poller ที่ใช้ targetApp default "WindowsUI" **ไม่มี filter เลย** สามารถเคลม job ของ AutomationType ไหนก็ได้ (เช่น "iOSApp"); มีแค่ poller ที่ใช้ target อื่นที่ไม่ใช่ WindowsUI เท่านั้นที่ถูกจำกัดให้ตรง target ของตัวเองหรือ WindowsUI fallback — พฤติกรรมนี้ตรงข้ามกับสมมติฐานแรกที่คิดว่า WindowsUI poller จะถูกจำกัดด้วย จึงต้องแก้เทสต์ที่เขียนผิดออกจากการรันจริง
- ยืนยันอีกครั้งว่าไม่มี capability-based routing จริง (`capabilities` รับมาแต่ไม่ใช้กรอง) และ `RequestedAgentId` จาก batch ไม่ถูกบังคับตอน claim — ทั้งสองจุดนี้ทำให้ AC เดิม "กระจายงานตาม capability/target" ยังไม่ตรงกับพฤติกรรมจริงทั้งหมด ควรเปิดเป็นงานปรับปรุงแยกถ้าต้องการ routing ที่บังคับจริง
- Build: `dotnet build ProMaxx2.QA.slnx` 0 warning/0 error; `dotnet test` ผ่านทั้งชุด 133/133 (เพิ่มจาก 126)

### 2026-08-27 — AUT-TEST-005: Retry tests

- ปิด `AUT-TEST-005`
- เพิ่ม `AutomationRetryTests.cs` 7 tests ครอบคลุม retry เดียวต่อความล้มเหลว 1 ครั้งพร้อม backoff, chain retry จนถึง MaxAttempts แล้วหยุด (ไม่มีรอบถัดไป), non-retryable UI failure เข้า MaintenanceRequired ไม่ retry, error code ที่ไม่รู้จักไม่ retry (case กลับ Ready), retry policy Disabled ปิด retry ทั้งหมด, unsafe action ที่ execute ผ่านไปแล้วบล็อก retry, duplicate completion report ไม่สร้าง retry ซ้ำ (อาศัย idempotency guard ที่เพิ่มตอนทำ AUT-TEST-004)
- Build: `dotnet build ProMaxx2.QA.slnx` 0 warning/0 error; `dotnet test` ผ่านทั้งชุด 126/126 (เพิ่มจาก 119)

### 2026-08-27 — AUT-TEST-004: Cancel/Timeout/Lease Recovery tests + bug fix

- ปิด `AUT-TEST-004`
- เพิ่ม `AutomationCancelTimeoutTests.cs` 9 tests: cancel Queued/Running execution, cancel ซ้ำ execution ที่ terminal แล้วต้อง throw, Timeout/AgentLost result classify ถูก, late result หลัง cancel ไม่ทับสถานะ, duplicate result report ไม่ประมวลผลซ้ำ (ไม่สร้าง retry execution ซ้ำซ้อน), domain guard บน `AutomationJob.Complete`/`AutomationExecution.Complete`
- **พบและแก้ bug จริงระหว่างเขียนเทสต์:** `AutomationExecution.Complete` และ `AutomationJob.Complete` เดิมไม่มี guard เช็คสถานะปัจจุบันก่อน overwrite — ผลคือถ้า agent ส่งผลลัพธ์มาช้า (late result) หลังจาก execution ถูก user cancel ไปแล้ว หรือส่งผลซ้ำ (duplicate report) ระบบจะ**เขียนทับสถานะ Cancelled/Failed เดิมแบบเงียบๆ** และอาจสร้าง retry execution ซ้ำซ้อนได้ — แก้โดยเพิ่ม guard `if (Status is not ("Queued" or "Running")) throw InvalidOperationException` ในทั้งสอง entity (defense-in-depth) และเพิ่มเช็คใน `CompleteExecutionAsync` ก่อนเรียก `execution.Complete` ให้ return สถานะปัจจุบันแบบ idempotent เมื่อ execution terminal ไปแล้ว แทนที่จะ error หรือ overwrite
- **ข้อสังเกตสำคัญ:** ยังไม่มี background lease-expiry watchdog ในโค้ด (ไม่มี `IHostedService`/`BackgroundService` ตรวจ Running job ที่ agent heartbeat หมดอายุแล้ว mark เป็น AgentLost อัตโนมัติ) — ส่วน "expired lease" ตาม AC เดิมยังไม่ implement จริง เป็น gap ที่ควรเปิดเป็นงานใหม่แยกต่างหาก (เกี่ยวโยงกับ AUT-TEST-003 ที่ก็ไม่มี lease token เช่นกัน)
- Build: ต้อง `dotnet build-server shutdown` และหยุด `ProMaxx2.QA.Api.exe` dev instance ที่ค้างรันอยู่ก่อน เพราะ lock DLL ทำให้ build ไม่ผ่าน (ได้รับอนุมัติจากผู้ใช้ก่อนหยุด process); หลังจากนั้น `dotnet build ProMaxx2.QA.slnx` 0 warning/0 error; `dotnet test` ผ่านทั้งชุด 119/119 (เพิ่มจาก 110)

### 2026-08-27 — AUT-TEST-003: Atomic Job Claim tests

- ปิด `AUT-TEST-003`
- เพิ่ม `AutomationTestFixtures.SeedReadyCaseAsync` helper (Draft→Validate→Approve จนได้ Ready case + approved version พร้อม request execution/job ต่อ)
- เพิ่ม `AutomationJobClaimTests.cs` 7 tests ครอบคลุม double-claim prevention, assign ถูก agent, agent ปิดใช้งาน claim ไม่ได้, ไม่มี job ว่าง, backoff job ในอนาคต claim ไม่ได้ยัง, priority ordering, domain guard `AutomationJob.Assign`
- **ข้อสังเกตสำคัญ:** acceptance criteria เดิมพูดถึง "lease token" แต่โค้ดจริงไม่มี field นี้ — ผู้ที่เขียน spec เดิมอาจตั้งใจให้มี lease token/expiry สำหรับ recovery (เกี่ยวข้องกับ AUT-TEST-004 Cancel/Timeout/Lease Recovery) แต่ปัจจุบันยังไม่ implement; เทสต์ที่เพิ่มยืนยัน invariant ที่มีอยู่จริง (sequential double-claim + priority + backoff) ด้วย EF InMemory ซึ่งไม่รองรับ transaction ดังนั้นยังไม่ครอบคลุม concurrent race จริงบน production DB
- Build: `dotnet build ProMaxx2.QA.slnx` 0 warning/0 error; `dotnet test` ผ่านทั้งชุด 110/110 (เพิ่มจาก 103)

### 2026-08-27 — AUT-TEST-001/002: DSL Validator + Case Workflow tests

- ปิด `AUT-TEST-001`, `AUT-TEST-002`
- เพิ่ม `AutomationTestFixtures.cs` shared seed helper (InMemory `QaDbContext`, seed Project/Module/Release/Build/Environment/TestCase, `CaseService`/`AgentService` factory, sample DSL) ใช้ร่วมกันในเทสต์ Automation module ต่อจากนี้
- เพิ่ม `AutomationDslValidatorTests.cs` 17 tests: required fields, unsupported DslVersion, step number rules (positive/unique/contiguous), unknown Action/Object/TestData reference ทั้งกรณี library ว่าง (bypass) และไม่ว่าง (reject)
- เพิ่ม `AutomationCaseWorkflowTests.cs` 4 tests: reject สร้าง Case จาก TestCase ที่ไม่ใช่ automation candidate, full flow Draft→Validate→Approve→Ready→MaintenanceRequired→AssignOwner→Resolve→NeedsReview, invalid DSL ทำให้ version Invalid และ case กลับ NeedsReview, resolve maintenance ที่ไม่ได้อยู่สถานะ MaintenanceRequired ต้อง throw
- **พบและแก้ build blocker:** `ProMaxx2.QA.UnitTests.csproj` ถูกเพิ่ม `Microsoft.EntityFrameworkCore.Sqlite` และ `Microsoft.AspNetCore.Mvc.Testing` (ยังไม่มีโค้ดใช้งานจริง) ซึ่งดึง `SQLitePCLRaw.lib.e_sqlite3` 2.1.11 ที่มี known vulnerability (NU1903) เข้ามา แล้วโดน root `Directory.Build.props` (`TreatWarningsAsErrors=true`) เปลี่ยนเป็น error ทำให้ restore/build ทั้ง test project ล้มเหลว — เอา 2 package ที่ยังไม่ได้ใช้ออกจนกว่าจะมีเทสต์ต้องใช้จริง (เช่น integration test ด้วย `WebApplicationFactory`)
- Build: `dotnet build ProMaxx2.QA.slnx` 0 warning/0 error; `dotnet test` ผ่านทั้งชุด 103/103 (เพิ่มจาก 79 เทสต์เดิม)

### 2026-08-26 — P0 Runtime: Verification, Maintenance, Failure Analysis, Retry, Quarantine, Failure Dashboard

- ปิด `AUT-P0-006` ถึง `AUT-P0-011`
- Migration ใหม่ `AddAutomationReliabilityAndVerification`: เพิ่มตาราง `AutomationObjectVerifications`, `AutomationRetryPolicySettings` (seed แถวเดียว MaxAttempts=2/BackoffSeconds=30/Enabled=true) และคอลัมน์ใหม่บน `AutomationExecutions` (ClassifiedFailureType/ClassifiedRecommendation/RetryOfExecutionId/RetryCount), `AutomationCases` (Maintenance*/Quarantine*), `AutomationActions` (RetrySafety) — apply สำเร็จบน dev DB จริง
- Domain: `AutomationCase.RequireMaintenance/AssignMaintenanceOwner/ResolveMaintenance/Quarantine/Unquarantine`, `AutomationExecution.SetClassification/MarkAsRetry`, `AutomationAction.RetrySafety`, entity ใหม่ `AutomationObjectVerification`, `AutomationRetryPolicySettings`
- `CompleteExecutionAsync` เรียก `AutomationFailureClassifier` ทุกครั้งที่ Fail/Timeout/AgentLost แล้ว persist ผล, ตัดสินใจ auto-retry ตาม Retry Policy + Unsafe Action check, สร้าง execution/job ใหม่พร้อม backoff ผ่าน `QueuedAt` ในอนาคต (ไม่ต้องมี background scheduler)
- แก้ bug เดิมใน `AutomationFailureClassifier`: guard เช็คเฉพาะ `Status == "Failed"` ทำให้ Timeout/AgentLost ไม่เคยถูกจำแนก (จึง retry ไม่เคยทำงานกับสถานะเหล่านี้) — ขยายเป็น `Failed`/`Timeout`/`AgentLost`
- API ใหม่: `objects/verify`, `objects/verifications`, `cases/{id}/maintenance/owner`, `cases/{id}/maintenance/resolve`, `cases/flaky-candidates`, `cases/{id}/quarantine`, `cases/{id}/unquarantine`, `failures/dashboard`, `failures/executions`, `settings/retry-policy` (GET/PUT), agent-facing `verifications/claim`, `verifications/result`
- Agent (`agent/ProMaxx2.Automation.*`): เพิ่ม `ObjectVerifier.cs`, `QaHubClient` เมธอด claim/report verification, คำสั่งใหม่ `runner verify --exe <path>`; build ผ่าน `ProMaxx2.Automation.slnx` ทั้งชุด — **ยังไม่ได้ทดสอบกับ ProMaxx2.exe จริง เพราะสภาพแวดล้อมนี้ไม่มี AUT/Windows session ให้รัน UI Automation — ต้องให้ QA ทดสอบภาคสนามก่อนใช้งานจริง**
- Frontend: Object Repository เพิ่มเลือกหลาย Object + ปุ่มตรวจสอบ + modal ผลตรวจสอบ; Case Detail เพิ่ม Maintenance Repair panel และ Quarantine panel; Cases tab เพิ่ม Flaky Candidates panel + Quarantine modal; Execution Detail แสดง classification ที่ persist แล้ว + retry badge; Jobs แสดง Retry Count; เพิ่ม tab ใหม่ "Failure Dashboard"; Action Library เพิ่ม Retry Safety; เพิ่มหน้าตั้งค่า Retry Policy ใน "การจัดการ"
- Tests: เพิ่ม 9 unit tests ใน `AutomationManagementTests.cs` (Maintenance/Quarantine/Classification/Retry/Verification/RetryPolicySettings) รวมทั้งไฟล์ 13 tests, test suite รวมผ่าน 79 tests
- Build: `dotnet build` (QA Hub solution + Agent solution แยกกัน), `npm.cmd run build`, `npm.cmd run lint`, boot-check API (swagger 200, ไม่มี DI error) ผ่านทั้งหมด

### 2026-08-26 — P0 Action/Object Management

- ปิด `AUT-P0-001` ถึง `AUT-P0-004`
- Action Library รองรับแก้ไข runtime metadata, Parameter Schema, Handler Key และเปิด/ปิด
- Object Repository รองรับแก้ Business Key/selector, เพิ่ม ObjectVersion, ป้องกัน Business Key ซ้ำ และเปิด/ปิด
- เพิ่ม server-side JSON validation และ error response สำหรับ update endpoints
- เพิ่ม `AutomationManagementTests.cs` จำนวน 4 tests; test suite รวมผ่าน 70 tests
- Frontend `npm.cmd run build` และ `npm.cmd run lint` ผ่าน

### 2026-08-26 — P0 Object Import

- ปิด `AUT-P0-005`
- Object Repository เพิ่ม Import Scanner modal รองรับ JSON/CSV จาก UI Inspector/AutomationId Scanner
- Preview แสดงสถานะ Ready/DuplicateKey/DuplicateAutomationId/Invalid และเลือก import เฉพาะรายการที่ต้องการได้
- Backend เพิ่ม `POST /api/v1/automation/objects/import` พร้อม validation และป้องกัน Business Key/AutomationId ซ้ำ
- Frontend `npm.cmd run build` และ backend `dotnet build` ผ่าน

### 2026-08-26 — สร้าง Tracker

- สร้าง Automation Development To-Do เป็นแหล่งติดตามงานคงค้างกลาง
- บันทึก baseline จากฐานข้อมูล: 11 Cases, 35 Executions, 23 Failed, 5 MaintenanceRequired, 5 Objects
- จัดลำดับงานเป็น P0 Runtime/Tests, P1 Suite/Schedule/Data/Regression และ P2 Monitoring/UX
- เพิ่มกฎใน `AGENTS.md` ให้ทุกงาน Automation ต้องอ่านและอัปเดตไฟล์นี้
